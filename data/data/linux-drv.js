QBANK.add([
{
  id: 'drv-001', cat: 'linux-drv', type: 'qa', level: 2, tags: ['字符设备'],
  q: '写一个字符设备驱动需要哪几步？',
  a: '**1. 申请设备号**\n```c\ndev_t devno;\nalloc_chrdev_region(&devno, 0, 1, \"mydev\");   /* 动态分配，推荐 */\n```\n主设备号标识驱动，次设备号区分同类型的多个设备。**不要用 `register_chrdev_region` 静态指定**，容易和已有设备冲突。\n\n**2. 实现 file_operations**\n```c\nstatic const struct file_operations fops = {\n    .owner          = THIS_MODULE,\n    .open           = mydev_open,\n    .release        = mydev_release,\n    .read           = mydev_read,\n    .write          = mydev_write,\n    .unlocked_ioctl = mydev_ioctl,\n    .poll           = mydev_poll,\n    .mmap           = mydev_mmap,\n};\n```\n用户态的 `open/read/write/ioctl` 最终会调到这里。\n\n**3. 注册 cdev**\n```c\ncdev_init(&my_cdev, &fops);\nmy_cdev.owner = THIS_MODULE;\ncdev_add(&my_cdev, devno, 1);\n```\n\n**4. 创建设备节点（自动）**\n```c\nmy_class = class_create(THIS_MODULE, \"myclass\");\ndevice_create(my_class, NULL, devno, NULL, \"mydev\");\n```\n这样 **udev 会自动在 `/dev/mydev` 创建节点**，不用手动 `mknod`。\n\n**5. 卸载时按相反顺序清理**\n```c\ndevice_destroy(my_class, devno);\nclass_destroy(my_class);\ncdev_del(&my_cdev);\nunregister_chrdev_region(devno, 1);\n```\n\n**关键实现细节**：\n\n- **用户空间数据必须用 `copy_to_user`/`copy_from_user`**，不能直接解引用用户传来的指针。这两个函数会检查地址合法性并处理缺页，**而且它们可能睡眠**，所以不能在原子上下文里调用。\n- **返回值语义**：read/write 返回**实际处理的字节数**，出错返回负的错误码（`-EINVAL`、`-EFAULT` 等）。返回 0 表示 EOF。\n- **并发保护**：多个进程可能同时打开同一个设备，共享的数据结构要加锁。\n- **阻塞与非阻塞**：检查 `filp->f_flags & O_NONBLOCK`，非阻塞时无数据要返回 `-EAGAIN`，阻塞时用等待队列睡眠。\n- **实现 `poll`** 才能让设备支持 select/epoll。\n\n**现代做法的补充**：如果设备挂在某个总线上（I2C、SPI、平台总线），应该写成对应的**总线驱动**（probe/remove 模型 + 设备树匹配），而不是在 `module_init` 里硬编码资源。字符设备接口只是它对用户空间暴露的形式。',
  followup: ['为什么必须用 copy_to_user 而不能直接解引用？', 'poll 方法怎么实现？', 'cdev 和 platform_driver 是什么关系？'],
  resume: true
},
{
  id: 'drv-002', cat: 'linux-drv', type: 'qa', level: 2, tags: ['设备树'],
  q: '设备树（Device Tree）是干什么的？驱动怎么和它匹配？',
  a: '**解决的问题**：ARM 平台的板级差异极大——同一个 SoC，不同板子上外设接在不同引脚、用不同中断号、挂不同的传感器。早期这些信息**硬编码在内核代码里**（`arch/arm/mach-xxx/board-yyy.c`），导致内核里塞满了几百个板级文件，Linus 为此发过著名的火。\n\n**设备树把"硬件描述"从内核代码里分离出来**，变成一份独立的数据文件（.dts → 编译成 .dtb），由 Bootloader 传给内核。**换一块板子只需要换 dtb，内核二进制不用改**。\n\n**dts 示例**：\n```dts\n&i2c1 {\n    status = \"okay\";\n    clock-frequency = <400000>;\n\n    mysensor@48 {\n        compatible = \"vendor,mysensor\";\n        reg = <0x48>;                     /* I2C 从机地址 */\n        interrupt-parent = <&gpio2>;\n        interrupts = <5 IRQ_TYPE_EDGE_FALLING>;\n        vdd-supply = <&reg_3v3>;\n        sample-rate = <100>;              /* 自定义属性 */\n    };\n};\n```\n\n**驱动侧的匹配**：\n```c\nstatic const struct of_device_id mysensor_of_match[] = {\n    { .compatible = \"vendor,mysensor\" },\n    { }\n};\nMODULE_DEVICE_TABLE(of, mysensor_of_match);\n\nstatic struct i2c_driver mysensor_driver = {\n    .driver = { .name = \"mysensor\", .of_match_table = mysensor_of_match },\n    .probe  = mysensor_probe,\n    .remove = mysensor_remove,\n};\n```\n\n**匹配靠 `compatible` 字符串**。内核解析 dtb 后为每个节点创建一个 `device`，然后在已注册的 driver 里找 `compatible` 匹配的，**匹配上就调用 `probe()`**。\n\n**probe 里读取属性**：\n```c\nstatic int mysensor_probe(struct i2c_client *client)\n{\n    struct device *dev = &client->dev;\n    u32 rate;\n\n    of_property_read_u32(dev->of_node, \"sample-rate\", &rate);\n    irq = of_irq_get(dev->of_node, 0);\n    regulator = devm_regulator_get(dev, \"vdd\");\n    ...\n}\n```\n\n**几个重要实践**：\n- **`compatible` 写成 `\"厂商,型号\"`**，且要有 fallback 列表（`compatible = \"vendor,chip-v2\", \"vendor,chip\"`），新驱动没有时能用旧驱动\n- **优先用 `devm_` 系列函数**（`devm_kzalloc`、`devm_gpiod_get`、`devm_request_irq`）。它们把资源绑定到 device，**probe 失败或 remove 时自动释放**，能消除一整类资源泄漏的错误处理代码\n- **`status = \"disabled\"`** 可以在不删除节点的情况下禁用某个设备\n- 调试时看 `/proc/device-tree/` 或 `/sys/firmware/devicetree/base/` 能查看内核实际解析到的设备树内容\n\n**排查提示**：驱动的 probe 没被调用，99% 是 `compatible` 字符串对不上、或者节点的 `status` 不是 okay、或者父节点被禁用了。',
  followup: ['devm_ 系列函数的原理是什么？', 'probe 没被调用怎么排查？', '设备树的 overlay 是干什么的？'],
},
{
  id: 'drv-003', cat: 'linux-drv', type: 'qa', level: 2, tags: ['中断', '下半部'],
  q: '中断的上半部和下半部是怎么划分的？softirq、tasklet、workqueue 怎么选？',
  a: '**为什么要分**：中断处理期间会屏蔽同级中断（甚至更多），处理时间越长，系统响应能力越差、丢中断的风险越高。所以要把工作分成两段：\n- **上半部（硬中断）**：只做最紧急、必须立刻完成的事——**读走数据、清中断标志**，然后调度下半部\n- **下半部**：耗时的处理（协议解析、数据上报、唤醒等待进程），在中断使能的情况下执行\n\n**三种下半部机制**：\n\n| | softirq | tasklet | workqueue |\n|---|---|---|---|\n| 执行上下文 | 中断上下文 | 中断上下文 | **进程上下文（内核线程）** |\n| 能否睡眠 | **不能** | **不能** | **能** |\n| 同类型能否并发 | **能**（多核上同时跑） | **不能**（同一 tasklet 串行） | 能 |\n| 使用难度 | 需要编译期静态注册 | 简单 | 简单 |\n| 典型用途 | 网络收发、块设备 | 一般驱动 | 需要睡眠的操作 |\n\n**选择逻辑（很简单）**：\n1. **下半部需要睡眠吗？**（要用互斥锁、要 `kmalloc(GFP_KERNEL)`、要访问用户空间、要等待硬件）\n   - **是 → workqueue**（唯一选择）\n   - 否 → 继续\n2. **需要在多核上并行执行同类型的下半部吗？**\n   - 是 → softirq（但 softirq 类型是内核编译期固定的，普通驱动通常用不了）\n   - 否 → **tasklet**（注意：tasklet 已被标记为废弃，新代码建议用 workqueue 或 threaded IRQ）\n\n**现代推荐：线程化中断（Threaded IRQ）**\n```c\nrequest_threaded_irq(irq, my_hardirq_handler, my_thread_fn,\n                     IRQF_ONESHOT, \"mydev\", dev);\n```\n- `my_hardirq_handler`：在中断上下文快速执行，返回 `IRQ_WAKE_THREAD` 表示要唤醒线程\n- `my_thread_fn`：**在专门的内核线程里执行，可以睡眠**\n- 优点：接口统一、可以调整线程优先级、**PREEMPT_RT 内核默认把所有中断都线程化**\n- **这是现在写新驱动最推荐的方式**\n\n**一个实用细节**：I2C/SPI 设备的中断处理几乎都必须用线程化中断或 workqueue——因为响应中断后往往要**通过 I2C/SPI 去读设备的状态寄存器，而 I2C/SPI 传输是会睡眠的**。在硬中断里做这件事会直接内核 panic。这是新手写传感器驱动最常踩的坑。',
  followup: ['为什么 I2C 设备的中断必须用线程化中断？', 'tasklet 为什么被废弃了？', 'PREEMPT_RT 下中断线程化有什么影响？'],
},
{
  id: 'drv-004', cat: 'linux-drv', type: 'single', level: 2, tags: ['内存分配'],
  q: '在中断上下文里分配内存，应该用哪个标志？',
  options: [
    'kmalloc(size, GFP_KERNEL)',
    'kmalloc(size, GFP_ATOMIC)',
    'vmalloc(size)',
    'malloc(size)'
  ],
  answer: [1],
  a: '**`GFP_ATOMIC`。**\n\n**区别**：\n- **`GFP_KERNEL`**：常规分配。内存不足时**会睡眠等待页面回收**（触发内存回收、甚至换出）。**在中断上下文里睡眠会导致内核崩溃或死锁。**\n- **`GFP_ATOMIC`**：**保证不睡眠**。可以动用紧急内存储备，但**分配失败的概率更高**，所以必须检查返回值。\n\n**其他常见标志**：\n- `GFP_DMA`：分配 DMA 可访问的低端内存（某些平台上 DMA 有地址范围限制）\n- `GFP_NOWAIT`：不睡眠也不用紧急储备，更保守\n- `GFP_USER`：为用户空间分配\n\n**`vmalloc` 为什么不行**：\n1. 它内部会睡眠\n2. 分配的是**虚拟地址连续但物理不连续**的内存，**不能用于 DMA**\n3. 需要修改页表，开销大\n\n`malloc` 是用户态函数，内核里根本没有。\n\n**判断当前是否在原子上下文**：`in_atomic()`、`in_interrupt()`。开发阶段开启 `CONFIG_DEBUG_ATOMIC_SLEEP`，内核会在原子上下文里睡眠时**打印完整的调用栈**，这是抓这类 bug 最有效的手段。\n\n**"原子上下文"包括哪些情况**（不只是中断）：\n- 硬中断和软中断处理函数\n- **持有自旋锁期间**\n- 关抢占期间（`preempt_disable()`）\n\n**所以持有自旋锁时也必须用 `GFP_ATOMIC`**，这一点很容易被忽略——代码可能在中断外，但只要拿着自旋锁就不能睡。\n\n**更好的实践**：中断上下文里**尽量不要分配内存**。\n- 预先分配好缓冲池，中断里只从池子里取\n- 或者用 per-CPU 变量、环形缓冲这类预分配的结构\n- 这样既避免了分配失败的风险，也让时间开销变得确定',
  followup: ['CONFIG_DEBUG_ATOMIC_SLEEP 能抓到哪些问题？', '为什么持有自旋锁时不能睡眠？', 'kmalloc 和 vmalloc 的适用场景分别是什么？'],
},
{
  id: 'drv-005', cat: 'linux-drv', type: 'qa', level: 2, tags: ['等待队列'],
  q: '驱动里怎么实现阻塞式的 read？等待队列怎么用？',
  a: '**目标**：用户调用 `read()` 时如果没有数据，让进程睡眠；数据到达（通常在中断里）时唤醒它。\n\n**驱动侧**：\n```c\nstatic DECLARE_WAIT_QUEUE_HEAD(my_wq);\nstatic int data_ready = 0;\n\n/* read 方法 */\nstatic ssize_t mydev_read(struct file *filp, char __user *buf,\n                          size_t count, loff_t *ppos)\n{\n    int ret;\n\n    if (filp->f_flags & O_NONBLOCK) {\n        if (!data_ready) return -EAGAIN;      /* 非阻塞模式立即返回 */\n    } else {\n        /* 阻塞等待，可被信号打断 */\n        ret = wait_event_interruptible(my_wq, data_ready != 0);\n        if (ret) return -ERESTARTSYS;          /* 被信号打断 */\n    }\n\n    /* 取数据 */\n    if (copy_to_user(buf, kbuf, len)) return -EFAULT;\n    data_ready = 0;\n    return len;\n}\n\n/* 中断里唤醒 */\nstatic irqreturn_t my_isr(int irq, void *dev)\n{\n    fill_kbuf();\n    data_ready = 1;\n    wake_up_interruptible(&my_wq);\n    return IRQ_HANDLED;\n}\n```\n\n**几个关键点**：\n\n**1. 用 `wait_event_interruptible` 而不是 `wait_event`**\n前者能被信号唤醒，用户可以用 Ctrl-C 或 `kill` 终止进程。用不可中断版本的话，**进程会进入 D 状态，连 `kill -9` 都杀不掉**——除非你有充分理由（比如操作中途不能被打断），否则一律用可中断版本。\n\n**2. 返回 `-ERESTARTSYS`**\n被信号打断时返回这个错误码，内核会根据信号处理方式决定是**自动重启系统调用**还是返回 `-EINTR` 给用户。这是标准做法。\n\n**3. 条件判断要能防止竞态**\n`wait_event_interruptible` 内部的实现是"先把自己加入等待队列并设为 TASK_INTERRUPTIBLE，再检查条件，条件不满足才调 schedule()"。**这个顺序保证了不会丢失唤醒**——如果在检查和睡眠之间数据到了，wake_up 会把已经在队列里的它设为 RUNNING，schedule() 就不会真的睡下去。**这和用户态条件变量必须原子地"解锁+挂起"是同一个道理。**\n\n**4. 条件变量要加保护**\n`data_ready` 被中断和进程同时访问，严格来说需要 `spin_lock_irqsave` 保护（或至少是 volatile / 原子类型）。\n\n**5. 还要实现 `poll` 方法**，否则用户态的 select/epoll 用不了：\n```c\nstatic __poll_t mydev_poll(struct file *filp, poll_table *wait)\n{\n    poll_wait(filp, &my_wq, wait);       /* 把等待队列注册给 poll，不会阻塞 */\n    return data_ready ? (EPOLLIN | EPOLLRDNORM) : 0;\n}\n```\n注意 `poll_wait` **不会睡眠**，它只是把当前进程挂到等待队列上；实际的阻塞由 VFS 层的 poll 机制统一处理。',
  followup: ['wait_event 和 wait_event_interruptible 应该怎么选？', '为什么 wait_event 的实现能避免丢失唤醒？', 'poll_wait 为什么不会阻塞？']
},
{
  id: 'drv-006', cat: 'linux-drv', type: 'qa', level: 2, tags: ['ioctl'],
  q: 'ioctl 是干什么的？命令号怎么定义？',
  a: '**用途**：read/write 只适合传输数据流，而设备往往需要**控制类操作**——设置波特率、查询状态、复位设备、切换模式。这些用 ioctl 表达最自然。\n\n**命令号的定义（必须用内核提供的宏）**：\n```c\n#define MY_IOC_MAGIC  「k」\n#define MY_RESET      _IO(MY_IOC_MAGIC, 0)              /* 无参数 */\n#define MY_GET_STATUS _IOR(MY_IOC_MAGIC, 1, int)        /* 读：内核→用户 */\n#define MY_SET_RATE   _IOW(MY_IOC_MAGIC, 2, int)        /* 写：用户→内核 */\n#define MY_EXCHANGE   _IOWR(MY_IOC_MAGIC, 3, struct cfg) /* 双向 */\n```\n\n**命令号的 32 位编码**：\n```\n[方向 2位][数据大小 14位][魔数 8位][序号 8位]\n```\n**为什么要这么编码而不是随便用 0、1、2**：\n1. **魔数（magic）区分不同驱动**。如果用户程序把 A 设备的 ioctl 命令误发给 B 设备，B 能通过魔数不匹配识别出来并返回 `-ENOTTY`，而不是错误地执行了自己的某个命令。**用裸数字的话，误操作会静默执行错误的命令**。\n2. **大小和方向信息**便于自动检查参数，也让 `strace` 之类的工具能解析。\n\n**实现**：\n```c\nstatic long mydev_ioctl(struct file *filp, unsigned int cmd, unsigned long arg)\n{\n    if (_IOC_TYPE(cmd) != MY_IOC_MAGIC) return -ENOTTY;\n\n    switch (cmd) {\n    case MY_SET_RATE: {\n        int rate;\n        if (copy_from_user(&rate, (int __user *)arg, sizeof(rate)))\n            return -EFAULT;\n        if (rate < MIN || rate > MAX) return -EINVAL;   /* 参数校验！ */\n        return set_rate(rate);\n    }\n    case MY_GET_STATUS: {\n        int st = get_status();\n        return copy_to_user((int __user *)arg, &st, sizeof(st)) ? -EFAULT : 0;\n    }\n    default:\n        return -ENOTTY;      /* 未知命令的标准返回值 */\n    }\n}\n```\n\n**注意事项**：\n1. **`.unlocked_ioctl` 而不是 `.ioctl`**。老的 `.ioctl` 会持有大内核锁（BKL），已经被移除了。\n2. **64 位内核 + 32 位用户程序**时，指针和 long 的大小不同，需要实现 `.compat_ioctl` 做转换。嵌入式上如果内核和应用位宽一致可以不用管。\n3. **参数必须严格校验**。ioctl 是内核攻击面的重灾区——用户传进来的任何值都不可信，范围、指针合法性都要检查。\n4. **`arg` 是 `unsigned long`**，传指针时要显式转换并加 `__user` 标记（便于 sparse 静态检查工具发现误用）。\n\n**替代方案**：简单的配置项用 **sysfs**（`/sys/class/xxx/yyy/attr`）更好——可以用 `cat`/`echo` 直接操作，方便调试和脚本化，不需要写专门的用户程序。复杂的、有原子性要求的操作才用 ioctl。',
  followup: ['为什么 ioctl 的魔数很重要？', 'sysfs 和 ioctl 分别适合什么场景？', 'compat_ioctl 什么时候需要实现？'],
},
{
  id: 'drv-007', cat: 'linux-drv', type: 'qa', level: 3, tags: ['DMA'],
  q: '驱动里怎么做 DMA？一致性 DMA 和流式 DMA 有什么区别？',
  a: '**核心问题**：DMA 控制器用的是**总线地址**（物理地址或经过 IOMMU 映射的地址），CPU 用的是**虚拟地址**；而且 CPU 有 Cache，DMA 通常不经过 Cache，两者看到的数据可能不一致。\n\n**一致性 DMA（Coherent / Consistent DMA）**\n```c\nvoid *cpu_addr;\ndma_addr_t dma_handle;\ncpu_addr = dma_alloc_coherent(dev, size, &dma_handle, GFP_KERNEL);\n/* cpu_addr 给 CPU 用，dma_handle 给 DMA 控制器用 */\ndma_free_coherent(dev, size, cpu_addr, dma_handle);\n```\n- 分配的内存被映射成**不可缓存（或硬件保证一致）**，CPU 和 DMA 随时看到相同的数据\n- **不需要手动做 Cache 同步**\n- 缺点：**CPU 访问慢**（绕过 Cache），且分配的是物理连续内存，大块可能失败\n- 适合：**生命周期长、双方频繁交替访问**的缓冲，如网卡的描述符环、DMA 描述符表\n\n**流式 DMA（Streaming DMA）**\n```c\ndma_addr_t handle = dma_map_single(dev, buf, size, DMA_TO_DEVICE);\n/* 启动 DMA，等完成 */\ndma_unmap_single(dev, handle, size, DMA_TO_DEVICE);\n```\n- 把**已有的内存**临时映射给 DMA 用，用完解除\n- **内核在 map/unmap 时自动做必要的 Cache 维护**：\n  - `DMA_TO_DEVICE`：map 时 **clean（回写）** Cache，保证 DMA 读到最新数据\n  - `DMA_FROM_DEVICE`：unmap 时 **invalidate** Cache，保证 CPU 读到 DMA 写的新数据\n- **关键约束：映射期间 CPU 不能访问这块内存**。必须 unmap 之后才能碰\n- 适合：**一次性的数据传输**，如网络包收发、块设备读写\n\n**选择**：\n- 长期存在、CPU 和设备都要频繁访问 → **一致性 DMA**\n- 一次性传输、传完就归 CPU → **流式 DMA**（性能更好，因为 CPU 访问时是走 Cache 的）\n\n**其他要点**：\n\n1. **必须设置 DMA 掩码**：\n   ```c\n   dma_set_mask_and_coherent(dev, DMA_BIT_MASK(32));\n   ```\n   告诉内核设备能寻址多少位。32 位 DMA 设备在 64 位系统上分配到高地址内存就没法用，内核靠这个掩码决定从哪里分配（必要时用 bounce buffer 中转）。\n\n2. **不要用栈上的缓冲区做 DMA**。栈可能不满足对齐和 Cache Line 要求，而且 `vmalloc` 的内存物理不连续，也不能直接用。\n\n3. **`dma_addr_t` 不是物理地址**。有 IOMMU 时它是设备视角的地址，不能拿去当物理地址用。\n\n4. **散列表（Scatter-Gather）**：数据分散在多个物理页时用 `dma_map_sg`，让 DMA 一次搬多段，避免拷贝到连续缓冲。',
  followup: ['为什么不能用栈上的缓冲做 DMA？', 'DMA 掩码设置不对会怎样？bounce buffer 是什么？', 'IOMMU 在 DMA 中起什么作用？'],
},
{
  id: 'drv-008', cat: 'linux-drv', type: 'single', level: 2, tags: ['模块'],
  q: '内核模块的 `module_init` 函数在什么时候被调用？',
  options: [
    '系统启动时一定会调用',
    '模块被 insmod/modprobe 加载时调用；如果编译进内核则在内核初始化阶段调用',
    '第一次打开设备文件时调用',
    '每次调用驱动接口时调用'
  ],
  answer: [1],
  a: '**取决于模块是编译成 .ko 还是编译进内核：**\n- **编译成模块（`CONFIG_XXX=m`）**：`insmod`/`modprobe` 加载时调用 init 函数，`rmmod` 时调用 exit 函数\n- **编译进内核（`CONFIG_XXX=y`）**：init 函数在**内核启动的初始化阶段**被调用，调用顺序由 initcall 的级别决定；此时 exit 函数根本不会被编译进去\n\n**initcall 的分级**（决定内核内建驱动的初始化顺序）：\n```c\ncore_initcall()       /* 最早 */\npostcore_initcall()\narch_initcall()\nsubsys_initcall()     /* 子系统，如总线 */\nfs_initcall()\ndevice_initcall()     /* = module_init，普通驱动 */\nlate_initcall()       /* 最晚 */\n```\n同级别内按**链接顺序**（即 Makefile 里的顺序）执行。**如果驱动 A 依赖驱动 B 先初始化，要么把 A 放到更晚的级别，要么用 deferred probe 机制。**\n\n**deferred probe**：现代内核推荐的做法——probe 时如果依赖的资源还没就绪（比如 regulator、clock 还没注册），**返回 `-EPROBE_DEFER`**，内核会把这个设备放进延迟队列，等有新驱动注册后重试。这样就不用手工排初始化顺序了。\n\n**其他要点**：\n\n1. **`module_init` 只是"注册驱动"，不等于"设备开始工作"**。现代的总线驱动模型里，init 函数通常只调用 `platform_driver_register()` 之类的注册函数；**真正的硬件初始化在 `probe()` 里**，而 probe 只在有匹配的设备时才被调用。\n\n2. **init 函数标记 `__init`**：这段代码放在 `.init.text` 段，**执行完后内核会释放这部分内存**。所以 `__init` 函数里不能存放会被后续使用的指针。同理 `__initdata` 标记的数据也会被释放。\n\n3. **init 返回非 0 表示失败**，模块加载失败，之前申请的资源必须在返回前全部释放（用 goto 错误处理链，或者用 `devm_` 系列自动管理）。\n\n4. **`MODULE_LICENSE(\"GPL\")` 必须写**，否则内核会标记为 tainted，而且**很多导出符号（EXPORT_SYMBOL_GPL）只对 GPL 模块可见**，不写会出现莫名其妙的链接失败。',
  followup: ['-EPROBE_DEFER 机制是怎么工作的？', '__init 标记有什么作用和限制？', '为什么不写 MODULE_LICENSE 会导致符号找不到？'],
},
{
  id: 'drv-009', cat: 'linux-drv', type: 'qa', level: 2, tags: ['sysfs'],
  q: 'sysfs 是什么？驱动怎么导出一个属性文件？',
  a: '**sysfs（`/sys`）是内核对象模型在用户空间的映射**，把内核里的设备、驱动、总线、类的层次关系以文件系统的形式暴露出来。\n\n**主要目录**：\n- `/sys/devices/` —— 所有设备的**真实层次结构**（按物理连接关系）\n- `/sys/class/` —— 按**功能分类**的视图（gpio、net、tty、leds），大多是指向 devices 的符号链接\n- `/sys/bus/` —— 按总线组织（i2c、spi、platform），下面有 devices 和 drivers\n- `/sys/module/` —— 已加载模块及其参数\n\n**导出一个属性**：\n```c\nstatic ssize_t rate_show(struct device *dev,\n                         struct device_attribute *attr, char *buf)\n{\n    return sysfs_emit(buf, \"%d\\n\", get_rate());\n}\n\nstatic ssize_t rate_store(struct device *dev, struct device_attribute *attr,\n                          const char *buf, size_t count)\n{\n    int val, ret;\n    ret = kstrtoint(buf, 10, &val);\n    if (ret) return ret;\n    if (val < MIN || val > MAX) return -EINVAL;   /* 校验！ */\n    set_rate(val);\n    return count;          /* 返回消耗的字节数 */\n}\nstatic DEVICE_ATTR_RW(rate);   /* 生成 dev_attr_rate */\n\n/* probe 里 */\ndevice_create_file(dev, &dev_attr_rate);\n```\n\n用户空间就能：\n```bash\ncat /sys/class/myclass/mydev/rate\necho 200 > /sys/class/myclass/mydev/rate\n```\n\n**sysfs 的设计约定（重要）**：\n- **一个文件只表示一个值**（"one value per file"）。不要在一个文件里输出一堆字段——那样解析麻烦，也不符合规范\n- **用文本格式**，方便 `cat`/`echo` 和 shell 脚本\n- 属性文件大小限制是 **一个页（4KB）**\n- 用 `sysfs_emit()` 而不是 `sprintf()`（前者带边界检查）\n\n**相比 ioctl 的优劣**：\n\n| | sysfs | ioctl |\n|---|---|---|\n| 调试 | **极方便**，cat/echo 即可 | 要写专门的程序 |\n| 脚本化 | **容易** | 困难 |\n| 自描述 | 好（文件名即语义） | 差（命令号是数字） |\n| 复杂/原子操作 | 不适合 | **适合** |\n| 大数据传输 | 不适合（4KB 限制） | 适合 |\n\n**实践建议**：**简单的配置项和状态查询一律用 sysfs**，调试效率高出一大截；只有需要原子性的复合操作、大数据传输、或者性能敏感的路径才用 ioctl。\n\n**相关的还有 debugfs（`/sys/kernel/debug`）**：专门用于调试信息，没有 sysfs 那么严格的规范约定，可以随便输出复杂内容。**调试用的东西应该放 debugfs 而不是 sysfs**，因为 sysfs 是稳定的用户接口，加进去就不好改了。',
  followup: ['debugfs 和 sysfs 应该怎么分工？', 'sysfs 的属性为什么限制一个页？', 'procfs、sysfs、debugfs 三者的定位有什么不同？'],
},
{
  id: 'drv-010', cat: 'linux-drv', type: 'qa', level: 2, tags: ['调试'],
  q: '内核驱动怎么调试？有哪些手段？',
  a: '**1. printk / 动态调试（最基础也最常用）**\n```c\npr_err(\"failed: %d\\n\", ret);\ndev_err(dev, \"init failed\\n\");      /* 自动带设备名前缀，推荐 */\ndev_dbg(dev, \"value = %d\\n\", val);  /* 动态调试，默认不输出 */\n```\n- 日志级别 KERN_EMERG(0) ~ KERN_DEBUG(7)，用 `dmesg` 查看\n- `dev_*` 系列会自动加上设备和驱动名，多设备时定位方便得多\n- **动态调试**：开启 `CONFIG_DYNAMIC_DEBUG` 后可以运行时开关某个文件/函数的 dev_dbg：\n  ```bash\n  echo 「file mydriver.c +p」 > /sys/kernel/debug/dynamic_debug/control\n  ```\n  **不用重新编译就能开日志**，这是排查现场问题的利器\n\n**2. 看 Oops / panic 信息**\n内核崩溃时会打印完整信息，关键看：\n- **PC 值和函数名**（`PC is at xxx+0x1c/0x80`）—— 崩在哪\n- **Call trace** —— 调用路径\n- **寄存器值** —— 常能看出是空指针（地址接近 0）还是野指针\n- 用 `addr2line -e vmlinux <地址>` 或 `scripts/faddr2line` 定位到源码行\n\n**3. ftrace（内核自带的追踪框架，非常强大）**\n```bash\ncd /sys/kernel/debug/tracing\necho function_graph > current_tracer\necho mydriver_* > set_ftrace_filter\necho 1 > tracing_on\ncat trace\n```\n能看到**函数调用关系和每个函数的耗时**，不用改一行代码。排查"卡在哪里"和性能问题极其有效。\n\n**4. 静态检查**\n- **sparse**：`make C=1`，检查 `__user`/`__iomem` 指针误用、字节序问题\n- **coccinelle**：`make coccicheck`，检查常见的错误模式\n- 编译时开 `W=1` 看更多警告\n\n**5. 内核调试选项（开发阶段应该全开）**\n- `CONFIG_DEBUG_ATOMIC_SLEEP`：原子上下文里睡眠时报警并打栈\n- `CONFIG_PROVE_LOCKING`（lockdep）：**自动检测潜在的死锁和锁顺序问题**，即使死锁没有实际发生。这个功能非常值钱\n- `CONFIG_DEBUG_KMEMLEAK`：内存泄漏检测\n- `CONFIG_KASAN`：内存越界和 use-after-free 检测\n- `CONFIG_DEBUG_OBJECTS`：对象生命周期检查\n\n**6. kgdb / KDB**\n通过串口或网络做内核级源码调试，能下断点、单步、查看变量。配置麻烦，但遇到难题时值得。\n\n**7. 硬件手段**\n- 逻辑分析仪抓总线波形，确认是驱动的问题还是硬件的问题\n- GPIO 翻转 + 示波器测中断延迟和函数耗时\n\n**排查顺序建议**：dmesg → 动态调试开日志 → ftrace 看调用路径 → 打开内核调试选项复现 → 最后才上 kgdb。从代价低的开始。',
  followup: ['lockdep 是怎么在死锁没发生时就检测出来的？', 'ftrace 的 function_graph 怎么用？', 'KASAN 的原理是什么？开销多大？'],
},
{
  id: 'drv-011', cat: 'linux-drv', type: 'single', level: 2, tags: ['并发'],
  q: '内核驱动中，中断处理函数和进程上下文都要访问同一个共享数据，应该用什么锁？',
  options: [
    'mutex_lock',
    'spin_lock_irqsave / spin_unlock_irqrestore',
    'spin_lock / spin_unlock',
    '不需要加锁'
  ],
  answer: [1],
  a: '**`spin_lock_irqsave` / `spin_unlock_irqrestore`。**\n\n**为什么不能用 mutex**：mutex 拿不到时会**睡眠**，而中断上下文不能睡眠。\n\n**为什么普通 `spin_lock` 不够**：\n设想进程上下文拿到了自旋锁，此时**同一个 CPU 上来了中断**，中断处理函数也要这把锁。中断会一直自旋等待，而持锁的进程因为被中断打断了根本没机会释放——**单核上直接死锁**。\n\n`spin_lock_irqsave` 在获取锁的同时**关闭本地 CPU 的中断**，杜绝了这种情况：\n```c\nunsigned long flags;\nspin_lock_irqsave(&my_lock, flags);\n/* 临界区：中断被关闭，其他 CPU 也进不来 */\nspin_unlock_irqrestore(&my_lock, flags);\n```\n\n**为什么是 `irqsave`/`irqrestore` 而不是简单的关开中断**：要**保存并恢复原来的中断状态**。如果调用者本来就在关中断的上下文里，无脑开中断会破坏外层的临界区。这和裸机里保存恢复 PRIMASK 是完全一样的道理。\n\n**锁的选择速查表**：\n\n| 场景 | 用什么 |\n|---|---|\n| 只有进程上下文，临界区可能睡眠 | `mutex` |\n| 只有进程上下文，临界区极短 | `spin_lock` |\n| 进程 + **软中断** | `spin_lock_bh` |\n| 进程 + **硬中断** | **`spin_lock_irqsave`** |\n| 只在中断上下文之间 | `spin_lock`（同一中断不会重入自己） |\n| 读多写少 | `rwlock` / **RCU** |\n| 简单计数器 | `atomic_t`，不用锁 |\n\n**其他要点**：\n- **持有自旋锁期间绝对不能睡眠**：不能 `kmalloc(GFP_KERNEL)`、不能 `copy_to_user`、不能调用任何可能阻塞的函数\n- **临界区要极短**。关中断期间系统对所有中断都不响应，直接影响实时性\n- **开 lockdep（`CONFIG_PROVE_LOCKING`）**，它能自动发现"同一把锁在不同上下文用了不同的加锁方式"这类问题，**在死锁真正发生之前就报警**',
  followup: ['spin_lock_bh 用在什么场景？', 'lockdep 能检测出哪些锁问题？', '什么时候可以用 atomic_t 代替锁？'],
},
{
  id: 'drv-012', cat: 'linux-drv', type: 'qa', level: 2, tags: ['I2C驱动'],
  q: '写一个 I2C 从设备驱动的基本框架是什么样的？',
  a: '**框架**：\n```c\nstruct my_data {\n    struct i2c_client *client;\n    struct mutex lock;\n    int irq;\n};\n\nstatic int my_read_reg(struct my_data *d, u8 reg, u8 *val)\n{\n    int ret = i2c_smbus_read_byte_data(d->client, reg);\n    if (ret < 0) return ret;\n    *val = ret;\n    return 0;\n}\n\nstatic int my_probe(struct i2c_client *client)\n{\n    struct my_data *d;\n    u8 id;\n    int ret;\n\n    d = devm_kzalloc(&client->dev, sizeof(*d), GFP_KERNEL);\n    if (!d) return -ENOMEM;\n    d->client = client;\n    mutex_init(&d->lock);\n    i2c_set_clientdata(client, d);\n\n    /* 读芯片 ID 验证设备真的存在 —— 这一步很重要 */\n    ret = my_read_reg(d, REG_WHO_AM_I, &id);\n    if (ret || id != EXPECTED_ID) {\n        dev_err(&client->dev, \"chip not found, id=0x%02x\\n\", id);\n        return -ENODEV;\n    }\n\n    /* 读设备树属性 */\n    device_property_read_u32(&client->dev, \"sample-rate\", &d->rate);\n\n    /* 中断：必须用线程化中断，因为处理里要走 I2C（会睡眠） */\n    if (client->irq > 0) {\n        ret = devm_request_threaded_irq(&client->dev, client->irq,\n                                        NULL, my_thread_fn,\n                                        IRQF_ONESHOT | IRQF_TRIGGER_FALLING,\n                                        \"mysensor\", d);\n        if (ret) return ret;\n    }\n\n    return my_hw_init(d);\n}\n\nstatic const struct of_device_id my_of_match[] = {\n    { .compatible = \"vendor,mysensor\" }, { }\n};\nMODULE_DEVICE_TABLE(of, my_of_match);\n\nstatic struct i2c_driver my_driver = {\n    .driver = { .name = \"mysensor\", .of_match_table = my_of_match },\n    .probe  = my_probe,\n};\nmodule_i2c_driver(my_driver);\n```\n\n**关键点**：\n\n1. **中断必须用 `request_threaded_irq`**。传感器中断来了之后要通过 I2C 去读状态寄存器，**而 I2C 传输会睡眠**——在硬中断里做会直接 panic。这是写传感器驱动最经典的坑。`IRQF_ONESHOT` 保证线程处理完之前中断保持屏蔽。\n\n2. **probe 里先读 ID 验证设备存在**。I2C 设备可能没焊、地址跳线错了、供电没上。读一个已知固定值的寄存器（WHO_AM_I / CHIP_ID）能立刻确认通路正常，比后面出各种怪问题好排查得多。\n\n3. **优先用 `devm_` 系列**：`devm_kzalloc`、`devm_request_threaded_irq`、`devm_regulator_get`。probe 失败时自动回滚，remove 时自动释放，**消除了一整套 goto 错误处理代码**。\n\n4. **用 `i2c_smbus_*` 系列而不是裸的 `i2c_transfer`**。前者封装了常见的"写寄存器地址 + 读数据"模式（内部用重复起始条件），代码更简洁也更不容易出错。需要非标准时序时才用 `i2c_transfer` 自己构造消息数组。\n\n5. **并发保护**：多个进程可能同时通过 sysfs 或字符设备访问，寄存器的读-改-写要加锁。\n\n6. **电源管理**：实现 `pm_ops` 的 suspend/resume，系统休眠时关掉传感器省电。\n\n7. **考虑用现成的子系统框架**：如果是传感器，用 **IIO（Industrial I/O）** 框架而不是自己造字符设备——它提供了标准的 sysfs 接口、缓冲、触发机制，用户空间有现成的库。同理输入设备用 input 子系统、LED 用 leds 子系统。**先找有没有对口的子系统，是写驱动前该做的第一件事。**',
  followup: ['IIO 子系统提供了哪些能力？', 'i2c_smbus_read_byte_data 底层是怎么实现的？', '为什么 IRQF_ONESHOT 对线程化中断很重要？'],
},
{
  id: 'drv-013', cat: 'linux-drv', type: 'qa', level: 3, tags: ['内存映射'],
  q: '驱动里怎么访问外设寄存器？为什么不能直接用物理地址？',
  a: '**不能直接用物理地址的原因**：内核开启了 MMU，运行在虚拟地址空间里。外设寄存器的物理地址（如 0x40011000）在内核的虚拟地址空间里**没有映射**，直接解引用会立刻触发缺页异常并 Oops。\n\n**正确做法：ioremap**\n```c\nvoid __iomem *base;\n\n/* 从设备树获取资源并映射，一步到位（推荐） */\nbase = devm_platform_ioremap_resource(pdev, 0);\nif (IS_ERR(base)) return PTR_ERR(base);\n\n/* 读写必须用专用访问函数 */\nu32 val = readl(base + REG_STATUS);\nwritel(0x01, base + REG_CTRL);\n\n/* devm_ 版本会自动 iounmap，手动映射的话要 iounmap(base) */\n```\n\n**为什么必须用 `readl`/`writel` 而不是直接解引用指针**：\n\n1. **加了内存屏障**。保证寄存器访问的顺序符合代码顺序，不被 CPU 乱序执行或写缓冲打乱。**寄存器操作的顺序往往是有语义的**（先配置再使能），乱序会导致硬件行为错误。\n2. **处理字节序**。`readl`/`writel` 是小端语义，大端 CPU 上会自动做转换。还有 `ioread32be` 等显式的大端版本。\n3. **平台适配**。某些架构上外设访问需要特殊指令（x86 的 in/out 端口访问），这些宏屏蔽了差异。\n4. **`__iomem` 标记 + sparse 检查**：`void __iomem *` 是特殊标记的指针类型，用 `make C=1` 跑 sparse 时会检查你有没有误把它当普通指针解引用。**这是个很有价值的静态检查。**\n\n**ioremap 做了什么**：在内核的 vmalloc 区建立一段虚拟地址到指定物理地址的映射，并把这段内存属性设为 **Device / Strongly-ordered**（不可缓存、不可推测访问、不合并）。这些属性对寄存器访问是必需的——如果被 Cache 缓存了，写寄存器可能停在 Cache 里没到硬件，读寄存器可能拿到旧值。\n\n**几个常见错误**：\n1. **忘了 `ioremap` 直接用物理地址** → Oops\n2. **用 `memcpy` 操作 `__iomem` 区域** → 应该用 `memcpy_toio`/`memcpy_fromio`，普通 memcpy 可能产生非对齐或被优化的访问方式，硬件不一定接受\n3. **忘了 `iounmap`** → 虚拟地址空间泄漏（用 `devm_` 版本可避免）\n4. **多个驱动映射同一段寄存器** → 应该用 `request_mem_region` 声明独占，避免冲突\n\n**寄存器地址从哪来**：现代驱动从**设备树的 `reg` 属性**获取，而不是硬编码：\n```dts\nmydev@40011000 {\n    compatible = \"vendor,mydev\";\n    reg = <0x40011000 0x1000>;\n};\n```\n这样同一个驱动能适配不同地址布局的 SoC。',
  followup: ['为什么 ioremap 的内存要设成 Device 属性？', 'readl/writel 和 readl_relaxed 有什么区别？', 'sparse 的 __iomem 检查能发现什么问题？'],
},
{
  id: 'drv-014', cat: 'linux-drv', type: 'single', level: 2, tags: ['字符设备'],
  q: '用户程序 `read(fd, buf, 100)`，驱动的 read 函数返回 30，用户程序看到的返回值是？',
  options: ['100', '30', '0', '-1'],
  answer: [1],
  a: '**30。驱动 read 的返回值就是实际读取的字节数，会原样传给用户。**\n\n**返回值约定**：\n- **正数**：实际读取/写入的字节数（**可以小于请求的数量**）\n- **0**：EOF（对普通文件是文件末尾；对设备通常表示没有更多数据）\n- **负数**：错误码的负值（`-EFAULT`、`-EINVAL`、`-EAGAIN`、`-ERESTARTSYS`）\n\n**这就是为什么用户程序必须循环读**：\n```c\nsize_t got = 0;\nwhile (got < need) {\n    ssize_t n = read(fd, buf + got, need - got);\n    if (n < 0) { if (errno == EINTR) continue; break; }\n    if (n == 0) break;              /* EOF */\n    got += n;\n}\n```\n**假设 read 一次就能读满是最常见的应用层 bug**。串口、socket、管道都可能返回部分数据，小数据量时看不出问题，一上量就出错。write 同理。\n\n**驱动侧的实现要点**：\n\n1. **正确更新文件偏移**：\n   ```c\n   *ppos += len;\n   ```\n   字符设备如果不支持定位，可以忽略 ppos，但要保持行为一致。\n\n2. **`copy_to_user` 返回值是"未拷贝成功的字节数"**，0 表示全部成功：\n   ```c\n   if (copy_to_user(buf, kbuf, len))\n       return -EFAULT;\n   ```\n   注意判断逻辑是"非 0 即失败"，容易写反。\n\n3. **不要一次返回超过用户请求的量**：`len = min(count, available)`。\n\n4. **阻塞语义要正确**：\n   - 阻塞模式且无数据 → 睡眠等待，至少返回 1 个字节才醒（或被信号打断）\n   - 非阻塞模式且无数据 → 返回 `-EAGAIN`\n\n5. **被信号打断时返回 `-ERESTARTSYS`**，让内核决定是重启系统调用还是返回 `-EINTR`。\n\n**一个容易忽略的点**：如果驱动**已经读到了一部分数据**然后被信号打断，应该**返回已读到的字节数**（正数），而不是错误码。丢弃已读数据会导致数据丢失。',
  followup: ['read 返回部分数据时应用层怎么正确处理？', 'copy_to_user 返回非 0 意味着什么？', '被信号打断但已读到部分数据，驱动该返回什么？']
},
{
  id: 'drv-015', cat: 'linux-drv', type: 'qa', level: 3, tags: ['电源管理'],
  q: '驱动怎么支持系统休眠和唤醒？runtime PM 是什么？',
  a: '**系统级电源管理（System Suspend）**\n```c\nstatic int my_suspend(struct device *dev)\n{\n    struct my_data *d = dev_get_drvdata(dev);\n    /* 保存必要的寄存器状态、停止 DMA、关闭时钟、断电 */\n    disable_irq(d->irq);\n    my_save_context(d);\n    regulator_disable(d->vdd);\n    return 0;\n}\n\nstatic int my_resume(struct device *dev)\n{\n    struct my_data *d = dev_get_drvdata(dev);\n    regulator_enable(d->vdd);\n    my_hw_init(d);                /* 硬件可能完全掉电了，要重新初始化 */\n    my_restore_context(d);\n    enable_irq(d->irq);\n    return 0;\n}\n\nstatic SIMPLE_DEV_PM_OPS(my_pm_ops, my_suspend, my_resume);\n/* 挂到 driver 结构里：.driver.pm = &my_pm_ops */\n```\n\n**关键认知**：suspend 时外设可能**完全掉电**，resume 后寄存器全是复位值。所以 resume 不能只"恢复几个变量"，而是要**完整地重新初始化硬件**。忘了这一点的表现是"休眠唤醒后设备不工作了"。\n\n**Runtime PM（运行时电源管理）**\n系统没有休眠，但**某个设备暂时不用，就单独把它关掉**。这是省电的大头——大部分时间系统是醒着的，但绝大多数外设是闲着的。\n\n```c\n/* probe 里启用 */\npm_runtime_set_active(dev);\npm_runtime_enable(dev);\npm_runtime_set_autosuspend_delay(dev, 1000);   /* 空闲 1 秒后自动挂起 */\npm_runtime_use_autosuspend(dev);\n\n/* 使用设备前 */\nret = pm_runtime_resume_and_get(dev);   /* 引用计数 +1，必要时唤醒设备 */\nif (ret < 0) return ret;\n\n... 操作硬件 ...\n\n/* 用完 */\npm_runtime_mark_last_busy(dev);\npm_runtime_put_autosuspend(dev);        /* 引用计数 -1 */\n```\n\n**机制**：内核为每个设备维护**引用计数**，计数归零且延时到期后自动调用 `runtime_suspend`；有人要用时自动 `runtime_resume`。**驱动只需要正确地 get/put，何时真正断电由框架决定。**\n\n**还要考虑的**：\n1. **父子设备的依赖**：子设备活跃时父设备不能挂起，框架会自动处理引用关系\n2. **唤醒源**：如果设备要能唤醒系统（按键、网络唤醒），要调 `device_init_wakeup()` 并在 suspend 时 `enable_irq_wake()`\n3. **suspend 的时机限制**：suspend 回调里**不能睡眠等待太久**，也不能依赖已经挂起的其他设备\n4. **`suspend_noirq`/`resume_noirq`**：在中断关闭后/开启前执行，用于处理需要严格时序的操作\n\n**调试**：\n```bash\necho 1 > /sys/power/pm_print_times     # 打印每个设备 suspend/resume 的耗时\ndmesg | grep -i suspend                # 看哪个设备 suspend 失败\ncat /sys/kernel/debug/pm_genpd/...     # 电源域状态\n```\n**"系统进不了休眠"的排查**：通常是某个设备 suspend 返回失败，或者有 wakelock 没释放，dmesg 里会明确指出是谁。',
  followup: ['runtime PM 的引用计数用错会有什么后果？', '怎么排查"系统进不了休眠"？', 'suspend_noirq 用在什么场景？']
},
{
  id: 'drv-016', cat: 'linux-drv', type: 'qa', level: 2, tags: ['用户态驱动'],
  q: '什么时候该写内核驱动，什么时候在用户态实现就够了？',
  a: '**先说结论：能在用户态解决就不要写内核驱动。** 内核驱动的开发、调试、维护成本高得多，出错的后果也严重得多（一个空指针就是整机崩溃）。\n\n**必须写内核驱动的情况**：\n1. **需要处理硬件中断**（用户态拿不到中断，只能轮询）\n2. **需要严格的时序控制**（用户态受调度影响，抖动可达毫秒级）\n3. **需要 DMA**（涉及物理地址和 Cache 一致性管理）\n4. **需要复用内核子系统**（网络协议栈、块设备层、V4L2、ALSA、IIO、input）\n5. **性能要求高**（避免用户态-内核态切换和数据拷贝）\n6. **多个进程需要共享访问同一硬件**，需要内核做仲裁和状态管理\n\n**用户态就够的情况**：\n1. **通过标准接口就能访问的设备**：\n   - I2C → `/dev/i2c-N` + ioctl（i2c-dev）\n   - SPI → `/dev/spidevX.Y`（spidev）\n   - GPIO → **libgpiod**（字符设备接口）\n   - 串口 → `/dev/ttySx` + termios\n   - USB → **libusb**（不用写内核 USB 驱动）\n2. **低频访问、时序要求不严**的设备（配置类芯片、EEPROM、低速传感器）\n3. **原型验证阶段**——先用户态跑通逻辑，确认硬件和算法没问题，再决定要不要下沉到内核\n\n**中间方案（很有价值但常被忽略）**：\n\n**UIO（Userspace I/O）**\n内核里只写一个极简的驱动（负责中断确认和内存映射），**中断和寄存器访问都交给用户态**：\n- 用户态通过 read `/dev/uioX` 等待中断\n- 通过 mmap 直接访问寄存器\n- **兼顾了"能处理中断"和"主逻辑在用户态好调试"**\n\n**VFIO**：更现代，带 IOMMU 保护，能安全地把整个设备（含 DMA）交给用户态。常用于虚拟化和 DPDK 这类高性能场景。\n\n**判断流程**：\n```\n需要中断/DMA/严格时序吗？\n  否 → 有现成的用户态接口吗？（i2c-dev/spidev/libgpiod/libusb）\n         有 → 用户态实现\n         无 → 考虑 UIO 或写内核驱动\n  是 → 有对口的内核子系统吗？（IIO/input/V4L2/ALSA）\n         有 → 基于该子系统写驱动（能复用大量基础设施）\n         无 → 只需要中断和寄存器访问 → UIO\n               需要复杂内核交互 → 完整的内核驱动\n```\n\n**面试价值**：这题考的是工程判断力。能说出"先用户态验证再下沉""优先找对口的内核子系统""UIO 是个折中方案"，比直接说"写驱动"要成熟得多。',
  followup: ['UIO 驱动的最小实现是什么样的？', 'spidev 和自己写 SPI 驱动各有什么优劣？', '怎么判断一个设备该用哪个内核子系统？'],
  resume: true
}
]);
