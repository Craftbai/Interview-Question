QBANK.add([
{
  id: 'code-017', cat: 'coding', type: 'qa', level: 2, tags: ['字符串', 'strlen'],
  q: '手写 strlen 和 strcmp。strlen 的返回值类型为什么是 size_t？strcmp 的返回值语义是什么？',
  a: '**strlen**：\n```c\nsize_t my_strlen(const char *s)\n{\n    size_t n = 0;\n    while (*s++)\n        n++;\n    return n;\n}\n```\n\n**strcmp**：\n```c\nint my_strcmp(const char *s, const char *t)\n{\n    while (*s && *t && *s == *t) {\n        s++;\n        t++;\n    }\n    return *(unsigned char *)s - *(unsigned char *)t;\n}\n```\n\n**strlen 为什么返回 size_t**：\n\n1. **长度不可能为负**。如果用 int，无法表示超过 INT_MAX 长度的字符串——在 64 位系统上这不算夸张，一个巨大的 mmap 文件映射出来的字符串可以轻松超过 2GB。\n\n2. **与指针运算的兼容性**。`strlen(s) + s` 这种表达式如果 strlen 返回 int，在 64 位系统上会把 int 提升到 long long 再做加法，虽然结果碰巧正确，但类型不匹配容易引起警告和混淆。`size_t` 和指针同宽，`size_t + ptr` 在 64 位上不会出问题。\n\n3. **与数组/字符串操作的接口一致**。`memcpy(buf, s, strlen(s))` 如果 strlen 返回 int，在 64 位系统上会做隐式转换，编译器经常报警告。`size_t` 是 `memcpy` 第三个参数的类型，用 `size_t` 就没有转换。\n\n**strcmp 的返回值语义**：\n\n| 返回值 | 含义 |\n|---|---|\n| < 0 | s 字典序小于 t |\n| 0 | 相等 |\n| > 0 | s 字典序大于 t |\n\n**返回值不一定是 -1、0、1**，而是**第一个不等字符的差值**。比如 `"abc"` 和 `"abd"` 返回 -1，`"a"` 和 `"b"` 也返回 -1，但 `"aaa"` 和 `"zzz"` 返回 `\'a\' - \'z\' = -25`。\n\n**为什么需要 cast 到 unsigned char***：\n\n如果 `char` 是 signed char 类型，`\'a\' - \'z\'` 没问题，但如果遇到 `char = -1`（比如 `\\xff` 的字节），`*(unsigned char *)s - *(unsigned char *)t` 会得到 `0 - 255 = -255`，而 `*s - *t`（两个 signed char 相减）可能得到 `255`（有符号数提升时出现符号扩展问题）。**必须 cast 到 unsigned char 再减**，这是 strcmp 实现中最隐蔽的 bug 来源。\n\n**实际项目中的替代方案**：\n- `strncmp`：限制比较长度，适合处理长度受限的缓冲区\n- `memcmp`：二进制比较，语义和 strcmp 相同但操作 `void *` 类型\n- `strcasecmp`：忽略大小写的比较（POSIX 扩展）',
  followup: ['为什么 strcmp 的返回值不是 -1/0/1？', 'char 是 signed 还是 unsigned 由谁决定？', '为什么嵌入式中 strcmp 常配合 strncmp 使用？']
},
{
  id: 'code-018', cat: 'coding', type: 'qa', level: 2, tags: ['memset', '内存'], resume: true,
  q: '手写 memset。能按字对齐优化吗？什么条件下可以做这个优化？',
  a: '**基础版本**：\n```c\nvoid *my_memset(void *dst, int value, size_t n)\n{\n    char *d = (char *)dst;\n    while (n--) *d++ = (char)value;   /* 标准库就是这么干的 */\n    return dst;\n}\n```\n\n**按字对齐的优化版本**：\n```c\nvoid *my_memset_aligned(void *dst, int value, size_t n)\n{\n    uint8_t byte = (uint8_t)value;    /* 只取低 8 位，和标准 memset 一致 */\n    char *d = (char *)dst;\n    uintptr_t addr = (uintptr_t)d;\n\n    /* 第一步：处理头部，直到地址按字对齐 */\n    size_t head = ((addr + sizeof(uintptr_t) - 1) & ~(sizeof(uintptr_t) - 1)) - addr;\n    if (head > n) head = n;\n    while (head--) *d++ = byte;\n\n    /* 第二步：按字填充 */\n    uintptr_t word;\n    switch (sizeof(uintptr_t)) {\n    case 8: word = (byte << 56) | (byte << 48) | (byte << 40) | (byte << 32)\n                   | (byte << 24) | (byte << 16) | (byte << 8) | byte; break;\n    case 4: word = (byte << 24) | (byte << 16) | (byte << 8) | byte; break;\n    case 2: word = (byte << 8) | byte; break;\n    default: word = byte; break;\n    }\n    uintptr_t *wd = (uintptr_t *)d;\n    size_t tail = n - head;\n    while (tail >= sizeof(uintptr_t)) {\n        *wd++ = word;\n        tail -= sizeof(uintptr_t);\n    }\n\n    /* 第三步：处理尾部 */\n    d = (char *)wd;\n    while (tail--) *d++ = byte;\n    return dst;\n}\n```\n\n**优化的前提条件**：\n\n1. **目标地址按字对齐**。`uintptr_t` 通常是 4 字节（32 位平台）或 8 字节（64 位平台）。如果不先处理头部直接按字填充，非对齐的字访问在 **ARM9、Cortex-R** 等平台上会触发 Abort Fault。Cortex-M3/M4/M7 通常允许非对齐访问（但性能下降）。\n\n2. **value 的低 8 位一致**。memset 的设计就是**只取低 8 位**，所以 `memset(buf, 0xFF, 4)` 和 `memset(buf, -1, 4)` 效果相同——四个字节的值都是 0xFF。按字填充时，字面量必须**四个（或八个）字节相同**，这正是 `0xFFFFFFFF` 或 `0xFF_FFFF_FF` 的构造原理。\n\n3. **value 不能是任意值**。memset 只能填充**单一字节值**，不能填充 `0x12345678` 这样的多字节模式。如果要填充特定多字节模式，用 memcpy 配合一个小数组：\n```c\nuint32_t pattern = 0x12345678;\nmemcpy(buf, &pattern, n % 4 == 0 ? n : n - (n % 4));\n```\n\n**为什么 libc 的 memset 要这么复杂**：\n\n在 32 位平台上，逐字节填充 1024 字节需要 1024 次写入。按 4 字节对齐后只需要 256 次——**性能提升 4 倍**。在嵌入式场景下，比如清零一块 4KB 的 DMA 缓冲区或者置零密钥材料，memset 的性能直接影响系统响应时间。\n\n**嵌入式实际使用 memset 的注意事项**：\n\n1. **volatile 内存不能用 memset 优化**。如果你要清零一个 volatile 的硬件寄存器数组，编译器可能把 memset 优化成一个循环，但如果编译器觉得目标不可优化（因为 volatile），它可能**完全跳过 memset** 或者改成逐字节操作。遇到这种情况要用**循环手动写入**。\n\n2. **敏感数据的清除**。memset 可能被编译器优化掉（"这段代码之后没有用到 buf，所以清零是死代码"）。正确做法是用 `memset_s`（C11 Annex K）或 `explicit_bzero`，或者用 volatile 指针。\n\n3. **你的 ECU 刷写系统里，flash 扇区的擦除本质上就是 memset 到 0xFF**——只不过硬件上是一条 FLASH_ERASE 指令，不是 CPU 写内存。',
  followup: ['memset 能填充 0x12345678 这样的多字节模式吗？', 'volatile 数组能安全地用 memset 清零吗？', 'Cortex-M 上非对齐的字写入有什么后果？']
},
{
  id: 'code-019', cat: 'coding', type: 'qa', level: 2, tags: ['字符串', '双指针'],
  q: '判断一个字符串是否是回文，忽略大小写和非字母字符。',
  a: '```c\n#include <ctype.h>\n\nint is_palindrome(const char *s)\n{\n    if (!s) return 1;   /* 空串也算回文 */\n\n    const char *left  = s;\n    const char *right = s + strlen(s) - 1;\n\n    while (left < right) {\n        while (left < right && !isalpha(*(unsigned char *)left)) left++;\n        while (left < right && !isalpha(*(unsigned char *)right)) right--;\n\n        if (tolower(*(unsigned char *)left) != tolower(*(unsigned char *)right))\n            return 0;\n        left++;\n        right--;\n    }\n    return 1;\n}\n```\n\n**要点**：\n\n1. **双指针从两端向中间收敛**。左指针跳过非字母字符，右指针也跳过，然后比较。每次比较成功，两边各前进一格。**只用一次 O(n) 遍历，空间 O(1)**。\n\n2. **tolower 前 cast 到 unsigned char**。`tolower` 和 `isalpha` 的参数是 `int`，但传入负值（`char` 溢出时）会导致未定义行为。这是 C 标准库函数中最容易被忽视的安全问题。\n\n3. **处理空串和单字符**。空串返回 1（数学上合理），单字符天然回文，双指针循环 `left < right` 不会执行，直接返回 1。\n\n4. **不等长跳过的情况**。如果左指针跳过了所有字符到达末尾，`left < right` 为假，循环退出，返回 1。例如输入 `"---"` 全部是非字母字符，返回 1（合理）。\n\n**常见用例**：\n\n- 车牌号识别后的回文校验\n- 嵌入式设备的序列号/验证码输入（用户不区分大小写和标点）\n- 通信协议中的校验字段\n\n**面试追问方向**：\n- 如果要求不预处理（不调 strlen）怎么办？——双指针可以直接用 `while(*s) s++; s--;` 找到末尾，不需要额外 O(n) 的 strlen 调用\n- 如果字符串非常大（比如 100MB 的日志文件）怎么办？——内存放不下时只能用磁盘上的分块比较，或者用 Rabin-Karp 的滚动哈希\n- 如果是 Unicode 字符串呢？——`isalpha` 和 `tolower` 只对 ASCII 有效，UTF-8 需要专门的 ICU 库处理\n\n**嵌入式场景下的注意事项**：\n\n如果你的车载设备需要判断用户输入的验证码是否是回文（比如某种对称加密的 nonce），**要注意输入缓冲区的长度限制**——用户输入可能超过缓冲区。另外，**不要在主循环中调用 strlen**，它也是 O(n) 的，双指针法可以直接在 O(n) 内完成而不需要额外的 strlen 调用。',
  followup: ['如果不调 strlen 能找到字符串末尾吗？', 'to_lower 对 Unicode 字符有效吗？', '处理超长字符串时有什么优化？']
},
{
  id: 'code-020', cat: 'coding', type: 'qa', level: 2, tags: ['字符串', '反转'],
  q: '原地反转字符串中的单词顺序，O(1) 额外空间。例如 "hello world foo" -> "foo world hello"。',
  a: '**核心思路**：先整体反转，再逐词反转。\n```c\nvoid reverse_word(char *s)\n{\n    if (!s) return;\n\n    /* 第一步：整体反转 */\n    int n = strlen(s);\n    for (int i = 0, j = n - 1; i < j; i++, j--) {\n        char t = s[i]; s[i] = s[j]; s[j] = t;\n    }\n\n    /* 第二步：逐词反转 */\n    char *start = s;\n    while (*start) {\n        /* 跳过前导空格 */\n        while (*start == \' \') start++;\n        if (!*start) break;\n\n        char *end = start;\n        while (*end && *end != \' \') end++;  /* 单词末尾 */\n\n        /* 反转这个单词 */\n        for (int l = start - s, r = end - s - 1; l < r; l++, r--) {\n            char t = s[l]; s[l] = s[r]; s[r] = t;\n        }\n        start = end;  /* 下一词的起点 */\n    }\n}\n```\n\n**以 "hello world foo" 为例**：\n\n| 步骤 | 内容 |\n|---|---|\n| 原始 | hello world foo |\n| 整体反转 | oof dlrow olleh |\n| 逐词反转 | foo world hello |\n\n**要点**：\n\n1. **为什么两步反转是对的**。单词内部的字符顺序是 `w[0], w[1], ..., w[k]`。整体反转后变成 `w[k], ..., w[1], w[0]`，再逐词反转一次就恢复了 `w[0], ..., w[k]`。但单词之间的相对顺序因为第一次整体反转已经倒过来了，所以最终结果就是单词顺序反转。\n\n2. **连续空格的处理**。`while (*start == \' \') start++;` 跳过连续空格，然后 `while (*end && *end != \' \')` 定位单词。连续空格会保留下来（因为反转操作只作用于单词内部），这是合理的——"hello  world" 反转后是 "world  hello"，中间的空格数不变。\n\n3. **边界情况**：\n   - 空字符串 `""` → 不变\n   - 只有空格 `"   "` → 不变\n   - 单个单词 `"hello"` → 不变\n   - 首尾有空格 `" hello "` → `"hello "`（前导空格被跳过但保留了尾部）\n\n4. **为什么用两步反转而不是开辟新数组**。题目要求 O(1) 空间，所以不能 `strtok` 后重新拼接。两步反转是**纯原地操作**，只需要一个临时变量 `t`。\n\n**嵌入式场景**：\n\n- 解析串口收到的指令时，经常需要反转参数顺序（比如 AT 命令的回复格式）\n- 显示设备上显示日志，最近的条目在最右边，反转后最新的在最左边\n- 嵌入式 Flash 存储字符串时，翻转后写入可以避免分配额外内存\n\n**面试追问**：\n- 如果要求去掉多余空格怎么办？——在逐词反转前先做一次压缩：用写指针遍历，跳过连续空格，只保留一个空格分隔\n- 如果是多字节字符（如 UTF-8）怎么办？——不能简单地按字节反转，需要按字符边界反转',
  followup: ['如何同时压缩多余空格？', 'UTF-8 字符串的反转怎么处理？', '如果要求只反转某几个单词怎么办？']
},
{
  id: 'code-021', cat: 'coding', type: 'qa', level: 2, tags: ['排序', '插入排序', '冒泡排序'], resume: true,
  q: '手写插入排序和冒泡排序。嵌入式系统里通常选哪个？为什么？',
  a: '**插入排序**：\n```c\nvoid insertion_sort(int *a, int n)\n{\n    for (int i = 1; i < n; i++) {\n        int key = a[i];\n        int j = i - 1;\n        while (j >= 0 && a[j] > key) {\n            a[j + 1] = a[j];\n            j--;\n        }\n        a[j + 1] = key;\n    }\n}\n```\n\n**冒泡排序**：\n```c\nvoid bubble_sort(int *a, int n)\n{\n    for (int i = 0; i < n - 1; i++) {\n        int swapped = 0;\n        for (int j = 0; j < n - 1 - i; j++) {\n            if (a[j] > a[j + 1]) {\n                int t = a[j]; a[j] = a[j + 1]; a[j + 1] = t;\n                swapped = 1;\n            }\n        }\n        if (!swapped) break;   /* 提前终止 */\n    }\n}\n```\n\n**为什么嵌入式选插入排序**：\n\n| 维度 | 插入排序 | 冒泡排序 |\n|---|---|---|\n| 最好时间 | **O(n)** 已排序 | O(n) 已排序 |\n| 最坏时间 | O(n²) | O(n²) |\n| 平均时间 | O(n²) | O(n²) |\n| 空间 | **O(1)** | O(1) |\n| 交换次数 | 少（只移一次） | 多（每次比较都可能交换） |\n| 缓存命中 | **好**（顺序访问） | 差（频繁跳跃） |\n| 代码行数 | 少 | 多 |\n| 递归 | **否** | 否 |\n\n**关键区别**：\n\n1. **交换 vs 移动**。冒泡排序每次发现逆序就交换（三次赋值），插入排序是先**移动**比 key 大的元素（一次赋值），最后**一次赋值**放下 key。**对于 MCU，每次赋值都要写 Flash/RAM，插入排序的写操作少得多**。\n\n2. **缓存友好**。插入排序的内层循环是连续内存访问，L1 cache 命中率高。冒泡排序的 `a[j]` 和 `a[j+1]` 虽然是连续的，但频繁交换会导致 cache line 频繁失效。\n\n3. **最好情况 O(n)**。插入排序在数据已部分有序时接近线性——**这在嵌入式中非常常见**（比如传感器数据缓慢变化，每次只需插入一个新值）。\n\n4. **无递归**。两者都不是递归的，这点比快速排序好。MCU 的栈很小（通常只有几 KB），递归深度不可控。\n\n5. **MISRA C 角度**。MISRA 不禁止这两种排序，但**禁止递归**，所以快速排序在嵌入式中需要手写迭代版。\n\n**实际嵌入式项目中的排序策略**：\n\n- **n < 20**：插入排序，代码短、无开销\n- **n 在 20~100 之间**：仍然用插入排序，或者**希尔排序**（插入排序的变种，增量序列可以是 n/2, n/4, ..., 1）\n- **n > 100**：考虑**归并排序**（迭代版，O(n log n)，额外需要 O(n) 空间）\n- **n 极大且有 MMU**：直接用 `qsort`（glibc 的 qsort 其实是 intro-sort，混合了快排、堆排和插入排序）\n\n**你的 ECU 刷写系统**中，差分包的校验数据量可能很大，但通常是在 PC 端用标准库排序；**STM32 端的传感器数据**如果要做排序滤波，插入排序绰绰有余。',
  followup: ['希尔排序是怎么优化插入排序的？', '嵌入式中什么时候必须用归并排序而不是快排？', 'qsort 在 glibc 中到底是什么算法？']
},
{
  id: 'code-022', cat: 'coding', type: 'single', level: 2, tags: ['二分查找', '边界'],
  q: '下面二分查找代码找的是"第一个大于 key 的元素"，请问哪行有 bug？\n\n```c\nint upper_bound(int *a, int n, int key) {\n    int lo = 0, hi = n - 1;       /* 1 */\n    while (lo < hi) {\n        int mid = lo + (hi - lo) / 2;\n        if (a[mid] > key)         /* 2 */\n            hi = mid;             /* 3 */\n        else\n            lo = mid + 1;         /* 4 */\n    }\n    return a[lo];                 /* 5 */\n}\n```',
  options: ['第 1 行：hi 应该等于 n 而不是 n-1', '第 3 行：hi = mid 会导致死循环', '第 4 行：lo = mid + 1 应该是 lo = mid', '第 5 行：lo 可能越界，应该先判断 lo < n'],
  answer: [0],
  a: '**选 A，第 1 行。**\n\n**问题在于 hi 的初始值**：`hi = n - 1` 把搜索范围限定在了 `[0, n-1]`。但"第一个大于 key 的元素"可能不存在——也就是说，**所有元素都小于等于 key**，此时应该返回一个"越界"信号（比如 -1 或 n）。\n\n正确的写法是 `hi = n`，让搜索范围是 `[lo, hi)`（左闭右开区间）：\n```c\nint upper_bound(int *a, int n, int key) {\n    int lo = 0, hi = n;           /* 搜索区间 [lo, hi) */\n    while (lo < hi) {\n        int mid = lo + (hi - lo) / 2;\n        if (a[mid] > key)\n            hi = mid;             /* mid 可能是答案，保留 */\n        else\n            lo = mid + 1;         /* mid 不是答案，排除 */\n    }\n    return lo;                    /* 返回索引，不是值 */\n}\n```\n\n**为什么 hi = n 是正确的**：\n\n当 `lo = hi` 时循环结束，此时 `lo` 就是第一个 `a[lo] > key` 的索引。如果所有元素都 ≤ key，则 `lo` 最终等于 `n`，**表示不存在这样的元素**。调用者需要检查 `lo < n`。\n\n**为什么 B 选项不对**：`hi = mid` 不会死循环，因为 `mid = lo + (hi-lo)/2` 总是向下取整，`hi > lo` 时 `mid < hi`，所以 `hi = mid` 一定会缩小范围。这和 `lo = mid` 不同——`lo = mid` 时 `mid >= lo`，确实可能死循环。\n\n**为什么 C 选项不对**：`lo = mid + 1` 是正确的。如果 `a[mid] <= key`，mid 及其左边的元素都不可能是答案，所以可以安全排除 mid。\n\n**为什么 D 选项部分对但不完整**：确实应该先判断 `lo < n`，但这建立在 hi = n 的基础上。如果 hi = n-1 且所有元素都 ≤ key，`lo` 最终停在 `n-1`，访问 `a[lo]` 不会越界，但逻辑错误——返回的是一个不大于 key 的值。\n\n**二分查找的不变量（invariant）思维**：\n\n写二分查找时，先定义清楚**循环不变量**：`a[lo-1] <= key` 且 `a[hi] > key`（如果存在的话）。然后确保每一步操作都维持这个不变量。这样边界就不会出错。\n\n**这题在面试中的延伸**：\n- 找第一个 ≥ key 的位置 → `a[mid] >= key ? hi = mid : lo = mid + 1`\n- 找最后一个 ≤ key 的位置 → `a[mid] <= key ? lo = mid : hi = mid - 1`\n- 找等于 key 的任意位置 → `a[mid] == key ? return mid : ...`\n\n**记住一个口诀**：`hi = mid` 时 mid 不能减 1（保留 mid），`lo = mid` 时 mid 必须加 1（防止死循环）。',
  followup: ['为什么 hi=mid 不会死循环而 lo=mid 会？', '怎么找"最后一个等于 key 的元素"？', '二分查找的不变量怎么证明正确性？']
},
{
  id: 'code-023', cat: 'coding', type: 'qa', level: 2, tags: ['链表', '合并'],
  q: '合并两个升序的单链表，返回新的升序链表。',
  a: '```c\ntypedef struct node { int val; struct node *next; } node_t;\n\nnode_t *merge_sorted(node_t *a, node_t *b)\n{\n    node_t dummy;                         /* 哑节点，简化边界处理 */\n    node_t *cur = &dummy;\n    dummy.next = NULL;\n\n    while (a && b) {\n        if (a->val <= b->val) {\n            cur->next = a;\n            a = a->next;\n        } else {\n            cur->next = b;\n            b = b->next;\n        }\n        cur = cur->next;\n    }\n\n    /* 把剩余部分直接挂上（至少一个是 NULL） */\n    cur->next = a ? a : b;\n    return dummy.next;\n}\n```\n\n**要点**：\n\n1. **哑节点（dummy node）是关键技巧**。如果没有 dummy，需要单独处理"第一个节点"的情况（a 和 b 谁更小就把谁当头）。用 dummy 后，所有逻辑统一在循环里，代码简洁且不出错。\n\n2. **`<=` 而不是 `<`**。相等时选 a 的节点，保持**稳定排序**（相等元素的相对顺序不变）。如果题目没说，用 `<` 也行，但 `<=` 更常见。\n\n3. **剩余部分直接挂上**。循环结束时，a 和 b 至少有一个是 NULL。另一个可能还有剩余节点，**这些节点已经是有序的**，直接挂到 cur 后面即可，不需要再排序。\n\n4. **时间复杂度 O(m+n)，空间 O(1)**。不需要分配新节点，只修改指针。\n\n**边界情况**：\n- 两个链表都为空 → 返回 NULL\n- 一个为空，一个不为空 → 返回非空的那个\n- 一个链表所有元素都小于另一个 → 直接拼接\n\n**嵌入式的实际考虑**：\n\n- **不能递归实现**。递归版代码更简洁，但栈深度等于链表长度之和，MCU 栈小，容易溢出\n- **如果链表很长**（比如几百个节点），可以考虑**归并排序的分治合并**思路——两两合并，逐步归并，每次合并的链表都比较短\n- **如果链表节点来自内存池**，合并后不需要释放任何节点，因为所有节点都还在使用中\n\n**面试追问方向**：\n- 如果链表是降序的怎么办？——先反转再合并，或者改写比较逻辑\n- 如果有三个有序链表要合并呢？——用最小堆（优先级队列）每次取出最小的，O(k·log k) 而不是 O(k·n)\n- 如果节点比较大（比如 256 字节），频繁移动节点开销大怎么办？——只移动节点指针，不移动节点数据',
  followup: ['三个有序链表怎么高效合并？', '节点很大时怎么减少移动开销？', '递归版合并为什么不适合嵌入式？']
},
{
  id: 'code-024', cat: 'coding', type: 'qa', level: 2, tags: ['链表', '双指针'], resume: true,
  q: '只遍历一次单链表，找出倒数第 k 个节点。',
  a: '**双指针法（快慢指针）**：\n```c\ntypedef struct node { int val; struct node *next; } node_t;\n\nnode_t *find_kth_from_end(node_t *head, int k)\n{\n    if (!head || k <= 0) return NULL;\n\n    node_t *fast = head;\n    node_t *slow = head;\n\n    /* fast 先走 k 步 */\n    for (int i = 0; i < k; i++) {\n        if (!fast) return NULL;     /* k 大于链表长度 */\n        fast = fast->next;\n    }\n\n    /* fast 和 slow 同时走，fast 到末尾时 slow 就是倒数第 k 个 */\n    while (fast) {\n        fast = fast->next;\n        slow = slow->next;\n    }\n    return slow;\n}\n```\n\n**核心思想**：\n\nfast 和 slow 之间始终保持 **k 个节点的间距**。当 fast 走到末尾（NULL）时，slow 恰好走了 `n - k` 步，也就是倒数第 k 个节点。\n\n**要点**：\n\n1. **为什么是"只遍历一次"**。fast 先走 k 步（k 次），然后 fast 和 slow 一起走 `n-k` 步，总共 `n` 步。对比：如果先遍历一遍数长度再遍历一遍定位，需要 `2n` 步。\n\n2. **k 大于链表长度的处理**。`for` 循环中 `if (!fast) return NULL` 提前返回。这是面试官想看到的边界检查。\n\n3. **k <= 0 的处理**。倒数第 0 个节点没有意义，返回 NULL。嵌入式项目中如果 k 来自外部输入（比如协议字段），**一定要检查有效性**。\n\n**常见变体**：\n- 找中间节点：fast 每次走两步，slow 每次走一步\n- 找中间节点（偶数个取第二个）：初始化时 slow=head, fast=head->next\n- 判断链表是否有环：同样用快慢指针，fast 每次两步，slow 每次一步，相遇则有环\n\n**面试追问方向**：\n- **如果有环怎么办**？先判断环是否存在，再判断倒数第 k 个节点是否在环内\n- **如果链表是双向链表怎么办**？可以直接从尾部往前数，不需要双指针技巧\n- **如果只能遍历一次且不能修改链表，但有额外 O(1) 空间**？双指针法就是答案\n\n**嵌入式实际场景**：\n\n在你的 **STM32 低功耗监测节点**中，传感器数据通过链表暂存，你需要定期取第 k 个采样值做趋势分析。用双指针法一次遍历搞定，不需要把链表拉直或者遍历两次。如果数据量不大（比如 50 个采样点），用简单的遍历计数也可以，但双指针法是更通用的解法。\n\n**另一个嵌入式场景**：CAN 总线上的报文队列，你需要取队列中倒数第 k 个报文做诊断分析（比如看最近一次错误的上下文），双指针法正好适用。',
  followup: ['如果链表有环，怎么找倒数第 k 个节点？', '判断链表是否有环的方法是什么？', '双指针法在双向链表上还有优势吗？']
},
{
  id: 'code-025', cat: 'coding', type: 'qa', level: 2, tags: ['链表', '相交'],
  q: '判断两个单链表是否相交，如果相交找出交点。',
  a: '```c\ntypedef struct node { int val; struct node *next; } node_t;\n\nnode_t *get_intersection(node_t *a, node_t *b)\n{\n    if (!a || !b) return NULL;\n\n    /* 第一步：计算两个链表的长度 */\n    int la = 1, lb = 1;\n    for (node_t *p = a; p->next; p++) la++;\n    for (node_t *p = b; p->next; p++) lb++;\n\n    /* 尾部不同则不相交 */\n    if (p->next && q->next) {\n        /* 上面代码有个问题，重新写 */\n    }\n\n    /* 重新整理 */\n    node_t *ta = a, *tb = b;\n    while (ta->next) ta = ta->next;\n    while (tb->next) tb = tb->next;\n    if (ta != tb) return NULL;              /* 尾部不同，不相交 */\n\n    /* 第二步：长的链表先走差值步 */\n    int diff = la - lb;\n    if (diff < 0) { diff = -diff; b = a; a = b; }  /* 确保 a 是较长的 */\n    while (diff--) a = a->next;\n\n    /* 第三步：同时走，相遇即为交点 */\n    while (a != b) {\n        a = a->next;\n        b = b->next;\n    }\n    return a;\n}\n```\n\n**修正后的完整版本**：\n```c\nnode_t *get_intersection(node_t *a, node_t *b)\n{\n    if (!a || !b) return NULL;\n\n    /* 计算长度并找到尾部 */\n    node_t *ta = a, *tb = b;\n    int la = 1, lb = 1;\n    while (ta->next) { ta = ta->next; la++; }\n    while (tb->next) { tb = tb->next; lb++; }\n\n    /* 尾部不同 → 不相交（Y 形链表尾部必须相同） */\n    if (ta != tb) return NULL;\n\n    /* 让长的链表先走差值步 */\n    int diff = la - lb;\n    if (diff < 0) { diff = -diff; node_t *tmp = a; a = b; b = tmp; }\n    while (diff--) a = a->next;\n\n    /* 同时走，相遇即为交点 */\n    while (a != b) {\n        a = a->next;\n        b = b->next;\n    }\n    return a;\n}\n```\n\n**要点**：\n\n1. **为什么看尾部就能判断相交**。如果两个链表相交，从交点开始它们共享相同的节点序列，**尾部必然是同一个节点**。如果尾部不同，说明它们是两条完全独立的链表。\n\n2. **为什么要先对齐**。假设链表 A 长 10 个节点，链表 B 长 7 个节点，交点在 B 的第 2 个节点（从头数）。如果同时从头开始走，A 走到交点时 B 才走到第 3 个节点，永远追不上。所以**必须让长的链表先走差值步**。\n\n3. **时间复杂度 O(m+n)，空间 O(1)**。两次遍历，不额外分配。\n\n**面试追问方向**：\n- **如果链表有环怎么办**？情况更复杂：两个链表可能在环外相交、在环内相交、或者一个有环一个无环\n- **能不能只用一次遍历**？有一种巧妙的方法叫**双指针交替法**：让 a 遍历完自己的链表后跳到 b 的头，b 遍历完自己的链表后跳到 a 的头。如果相交，两个指针会在交点相遇（因为 `la + lb` 的总路程相等）。\n- **如果链表节点带有环，如何找到环的入口**？快慢指针法——相遇后让一个指针回到头，两个指针每次都走一步，再次相遇就是入口\n\n**双指针交替法的代码**（面试加分项）：\n```c\nnode_t *get_intersection_fast(node_t *a, node_t *b)\n{\n    node_t *pa = a, *pb = b;\n    while (pa != pb) {\n        pa = pa ? pa->next : b;\n        pb = pb ? pb->next : a;\n    }\n    return pa;\n}\n```\n`pa` 走的路径是 `a → b`，`pb` 走的路径是 `b → a`，总长度都是 `la + lb`。如果相交，它们必然在交点相遇；如果不相交，它们会在 NULL 相遇（返回 NULL）。',
  followup: ['双指针交替法为什么能保证在交点相遇？', '有环的情况下怎么判断两个链表相交？', '怎么找到环的入口节点？']
},
{
  id: 'code-026', cat: 'coding', type: 'qa', level: 3, tags: ['字符串', 'itoa'],
  q: '手写 itoa（十进制整数转字符串），处理负数和 INT_MIN。',
  a: '```c\n#include <limits.h>\n\nchar *my_itoa(int val, char *buf, size_t size)\n{\n    if (!buf || size < 2) return buf;\n\n    /* 预留空间：最多 11 个字符（"-" + 10 位数字 + "\\0"） */\n    if (size < 12) return buf;\n\n    int neg = 0;\n    if (val < 0) {\n        /* 关键：INT_MIN 的绝对值是 2147483648，超过 INT_MAX */\n        /* 不能用 abs(val)，会溢出 */\n        neg = 1;\n        val = -(val + 1);                       /* val = -val - 1，避免溢出 */\n    }\n\n    /* 逐位提取，从低位到高位 */\n    char *p = buf;\n    do {\n        *p++ = (val % 10) + \'0\';\n        val /= 10;\n    } while (val > 0);\n\n    /* 如果是负数，补上 "-"（此时 val == 1，因为 INT_MIN 被减了 1） */\n    if (neg) *p++ = \'-\';\n\n    /* 反转 */\n    size_t len = p - buf;\n    for (size_t i = 0, j = len - 1; i < j; i++, j--)\n    {\n        char t = buf[i]; buf[i] = buf[j]; buf[j] = t;\n    }\n\n    *p = \'\\0\';\n    return buf;\n}\n```\n\n**INT_MIN 的陷阱**：\n\n`INT_MIN = -2147483648`，它的绝对值是 `2147483648`，超过了 `INT_MAX = 2147483647`。所以 `abs(INT_MIN)` 是未定义行为（UB）。\n\n**解决办法**：\n```c\nval = -(val + 1);    /* INT_MIN → 2147483647 */\n```\n这样 `val` 变成了正数 2147483647。然后在末尾补上 `\'-\'` 即可。注意这里 `-(val + 1)` 的写法——先加 1 再取反，避免了 `-(INT_MIN)` 这个 UB。\n\n**另一种更简洁的写法（全程用负数累加）**：\n```c\nchar *my_itoa(int val, char *buf, size_t size)\n{\n    if (!buf || size < 12) return buf;\n\n    int neg = 0;\n    if (val < 0) { neg = 1; val = -val; }  /* INT_MIN 取反后是 -2147483648，仍然是负数 */\n\n    char *p = buf;\n    do {\n        *p++ = (char)(\'0\' - (val % 10));    /* 用减法，因为是负数 */\n        val /= 10;\n    } while (val != 0);\n\n    if (neg) *p++ = \'-\';\n\n    /* 反转 */\n    size_t len = p - buf;\n    for (size_t i = 0, j = len - 1; i < j; i++, j--)\n    {\n        char t = buf[i]; buf[i] = buf[j]; buf[j] = t;\n    }\n    *p = \'\\0\';\n    return buf;\n}\n```\n\n**这个写法的关键**：`val % 10` 对负数的结果是**负的余数**（C99 规定），比如 `-123 % 10 = -3`。所以 `\'0\' - (val % 10)` 得到 `\'3\'`，不需要 `+` 号。\n\n**要点**：\n\n1. **缓冲区大小必须检查**。`int` 转字符串最长 12 字节（"-" + 10 位数字 + "\\0"），所以至少要 12 字节。\n\n2. **用 `do-while` 而不是 `while`**。这样可以正确处理 `val = 0` 的情况——`do-while` 至少执行一次，输出 `"0"`。如果用 `while`，`0 % 10 = 0` 但 `val = 0` 会直接退出循环，输出空字符串。\n\n3. **为什么要反转**。`123 % 10 = 3`，先得到的是最低位，所以生成的字符串是 `"321"`，需要反转。\n\n**嵌入式实际使用 itoa 的场景**：\n\n- **LCD/OLED 屏幕显示传感器数值**\n- **CAN 报文的状态码转字符串用于调试**\n- **日志系统中格式化整数输出**\n- **Bootloader 中版本号显示**\n\n**为什么不用 sprintf**：`sprintf(buf, "%d", val)` 在嵌入式中代价太大——链接完整的 printf 实现可能增加 20-40KB Flash，而 itoa 只有十几行代码。',
  followup: ['INT_MIN 取反后为什么还是负数？', 'C99 中负数取模的结果是正还是负？', '为什么 sprintf 在嵌入式中代价大？']
},
{
  id: 'code-027', cat: 'coding', type: 'qa', level: 3, tags: ['堆', '定时器'],
  q: '用最小堆实现一个软件定时器管理器：支持添加定时器、删除定时器、取到期定时器。',
  a: '```c\n#define MAX_TIMERS 32\n\ntypedef struct {\n    uint32_t expire_ms;     /* 到期时间戳（毫秒） */\n    void    *user_data;     /* 用户回调关联的数据 */\n    int      enabled;       /* 是否仍有效 */\n} timer_t;\n\ntypedef struct {\n    timer_t  heap[MAX_TIMERS];\n    uint8_t  size;\n} timer_mgr_t;\n\n/* ---------- 堆操作 ---------- */\n\nstatic void swim(timer_mgr_t *mgr, int i)\n{\n    while (i > 0) {\n        int parent = (i - 1) / 2;\n        if (mgr->heap[parent].expire_ms > mgr->heap[i].expire_ms) {\n            timer_t tmp = mgr->heap[parent];\n            mgr->heap[parent] = mgr->heap[i];\n            mgr->heap[i] = tmp;\n            i = parent;\n        } else {\n            break;\n        }\n    }\n}\n\nstatic void sink(timer_mgr_t *mgr, int i)\n{\n    while (1) {\n        int smallest = i;\n        int left  = 2 * i + 1;\n        int right = 2 * i + 2;\n\n        if (left < mgr->size && mgr->heap[left].expire_ms < mgr->heap[smallest].expire_ms)\n            smallest = left;\n        if (right < mgr->size && mgr->heap[right].expire_ms < mgr->heap[smallest].expire_ms)\n            smallest = right;\n\n        if (smallest != i) {\n            timer_t tmp = mgr->heap[i];\n            mgr->heap[i] = mgr->heap[smallest];\n            mgr->heap[smallest] = tmp;\n            i = smallest;\n        } else {\n            break;\n        }\n    }\n}\n\n/* ---------- 公开接口 ---------- */\n\nint timer_add(timer_mgr_t *mgr, uint32_t delay_ms, void *data)\n{\n    if (mgr->size >= MAX_TIMERS) return -1;\n\n    mgr->heap[mgr->size].expire_ms = get_tick_ms() + delay_ms;\n    mgr->heap[mgr->size].user_data = data;\n    mgr->heap[mgr->size].enabled = 1;\n    swim(mgr, mgr->size++);\n    return 0;\n}\n\nint timer_remove(timer_mgr_t *mgr, int index)\n{\n    if (index < 0 || index >= mgr->size) return -1;\n\n    /* 交换到末尾并弹出（O(1) 删除，但会打乱堆序）*/\n    mgr->heap[index] = mgr->heap[--mgr->size];\n    /* 重新调整：可能需要上浮或下沉 */\n    swim(mgr, index);\n    sink(mgr, index);\n    return 0;\n}\n\n/* 取所有已到期的定时器 */\nint timer_expire(timer_mgr_t *mgr, uint32_t now_ms, timer_t *out, int max_out)\n{\n    int count = 0;\n    while (mgr->size > 0 && mgr->heap[0].expire_ms <= now_ms && count < max_out) {\n        out[count++] = mgr->heap[0];\n        mgr->heap[0].enabled = 0;               /* 标记已消费 */\n        mgr->heap[0] = mgr->heap[--mgr->size];  /* 弹出堆顶 */\n        sink(mgr, 0);                           /* 恢复堆序 */\n    }\n    return count;\n}\n```\n\n**要点**：\n\n1. **为什么用最小堆**。最小堆的堆顶始终是**最早到期**的定时器，取到期定时器是 O(1) 的。添加是 O(log n)，删除是 O(log n)。\n\n2. **数组实现而不是指针实现**。静态数组 `MAX_TIMERS` 个元素，下标 i 的左右子节点分别是 `2i+1` 和 `2i+2`，父节点是 `(i-1)/2`。**零内存分配**，符合嵌入式要求。\n\n3. **enabled 标志的作用**。定时器的到期时间可能因为重新调度而改变，或者用户可能在定时器到期前取消它。标记 `enabled = 0` 后可以跳过执行回调，而不必从堆中立即删除（删除是 O(log n)，标记是 O(1)）。\n\n4. **timer_remove 的实现细节**。先把要删除的元素和末尾元素交换，再 pop。但交换过来的元素可能破坏堆序，所以需要 `swim` + `sink` 重新调整。实际上只需要其中一个（根据交换来的元素和原位置元素的大小关系决定），但两个都调是安全的。\n\n**嵌入式实际使用**：\n\n- **FreeRTOS 的 vTaskDelayUntil / vTaskDelay** 底层就用类似机制\n- **STM32 的软件定时器**（比如 HAL 库的 `HAL_Delay` 配合超时轮询）\n- **CAN 诊断的超时控制**（比如 ISO-TP 帧间隔超时）\n- **看门狗的重载计时**\n\n**可能的追问**：\n- **如果要支持高精度微秒级定时器怎么办**？用 64 位时间戳，或者用硬件定时器 + 软件补偿\n- **如果要支持周期性定时器（到期后自动重启）怎么办**？在 `timer_expire` 中检测到到期时，根据周期重新计算 `expire_ms`\n- **如果定时器数量很大（几千个）怎么办**？最小堆的 O(log n) 增长很慢，1000 个定时器只需要 10 层，依然很快。但如果需要频繁插入删除，可以用**配对堆**或**斐波那契堆**',
  followup: ['周期性定时器怎么实现？', '定时器数量上千时最小堆还够用吗？', '高精度微秒级定时器怎么实现？']
},
{
  id: 'code-028', cat: 'coding', type: 'qa', level: 3, tags: ['链表', 'container_of', '内核'], resume: true,
  q: '实现内核风格的内嵌式双向链表：定义 list_head、实现 list_add/list_del、用 list_entry 遍历。',
  a: '```c\n#include <stddef.h>\n\ntypedef struct list_node {\n    struct list_node *prev;\n    struct list_node *next;\n} list_node_t;\n\n#define LIST_HEAD_INIT(name) { &(name), &(name) }\n#define LIST_HEAD(name)      list_node_t name = LIST_HEAD_INIT(name)\n\nstatic inline void list_init(list_node_t *h)\n{\n    h->prev = h->next = h;\n}\n\nstatic inline void list_add(list_node_t *h, list_node_t *n)\n{\n    n->next     = h->next;\n    n->prev     = h;\n    h->next->prev = n;\n    h->next     = n;\n}\n\nstatic inline void list_del(list_node_t *n)\n{\n    n->prev->next = n->next;\n    n->next->prev = n->prev;\n    n->prev = n->next = NULL;                   /* 安全清理 */\n}\n\nstatic inline int list_empty(const list_node_t *h)\n{\n    return h->next == h;\n}\n\n/* 从 list_node_t 指针反推包含它的结构体指针 */\n#define list_entry(ptr, type, member)  \\\n    ((type *)((char *)(ptr) - offsetof(type, member)))\n\n/* 遍历宏 */\n#define list_for_each(pos, head) \\\n    for (pos = (head)->next; pos != (head); pos = pos->next)\n\n#define list_for_each_safe(pos, tmp, head) \\\n    for (pos = (head)->next, tmp = pos->next; \\\n         pos != (head); \\\n         pos = tmp, tmp = pos->next)\n```\n\n**使用示例**：\n```c\ntypedef struct {\n    int id;\n    char name[32];\n    list_node_t list;     /* 内嵌的链表节点 */\n} device_t;\n\nLIST_HEAD(devices);\n\ndevice_t *dev = malloc(...);\nlist_add(&devices, &dev->list);   /* 插入 */\n\nlist_node_t *pos;\nlist_for_each(pos, &devices) {\n    device_t *d = list_entry(pos, device_t, list);   /* 拿到 device_t 指针 */\n    printf("id=%d, name=%s\\n", d->id, d->name);\n}\n```\n\n**设计要点**：\n\n1. **为什么 list_head 是空的**。`list_head` 只存两个指针，不含数据。这样它可以被**嵌入到任何结构体中**，与结构体本身的内容无关。这是 Linux 内核链表设计的核心思想——**链表与数据分离**。\n\n2. **`container_of`（即 list_entry）的原理**。已知某个成员的地址 `ptr`，减去该成员在结构体中的偏移 `offsetof(type, member)`，就得到了结构体的起始地址。`offsetof` 通过 `(type *)0->member` 获取偏移。\n\n3. **为什么 `list_for_each_safe` 需要两个指针**。如果在遍历时调用 `list_del(pos)`，`pos` 已经被 unlink，`pos->next` 是无效的（被设为 NULL）。所以需要提前保存 `tmp = pos->next`。\n\n4. **双向循环链表的特性**。头节点的 `prev` 指向最后一个节点，`next` 指向第一个节点。遍历到头就回到头，不需要额外判断。这使得**插入和删除操作的代码完全对称且简洁**。\n\n5. **为什么 Linux 内核大量使用这种设计**。因为内核中有大量不同结构体（socket、inode、task_struct 等）需要被组织成链表。如果把链表节点放在结构体内部，就可以**不用修改原有结构体**就给它加上链表功能——这正是"内嵌式"的优势。\n\n**嵌入式实际使用**：\n\n- **你的 i.MX6ULL 语音终端**中，音频缓冲区可以用链表组织，播放线程和采集线程各自操作\n- **STM32 传感器节点**中，传感器驱动注册表可以用这种链表实现\n- **多协议工业网关**中，每个通信通道（RS485、CAN、MQTT）的信息结构可以挂在同一个链表上统一管理\n\n**面试追问方向**：\n- **list_entry 为什么用 `char *` 而不是 `void *`**？`void *` 不能做指针算术（没有字节级的步长），必须转成 `char *` 才能减偏移\n- **list_del 之后为什么要把 prev/next 置 NULL**？防止悬垂指针继续使用，这是安全最佳实践（虽然内核代码中经常省略这一步以追求极致性能）\n- **如果遍历中要删除多个节点怎么办**？用 `list_for_each_safe`，或者先收集待删除的节点再统一删除',
  followup: ['为什么 list_entry 要用 char* 做指针算术？', 'list_del 后置 NULL 是内核做的吗？', 'hlist（哈希链表）和普通双向链表有什么区别？']
},
{
  id: 'code-029', cat: 'coding', type: 'qa', level: 2, tags: ['字符串', '命令行解析'],
  q: '写一个 shell 风格的命令行参数解析器：按空格切分，支持单引号和双引号包裹的参数（如 `a "b c" \'d e\'` -> ["a", "b c", "d e"]）。',
  a: '```c\n#include <ctype.h>\n\nint parse_args(const char *cmd, char **argv, int max_argc)\n{\n    int argc = 0;\n    const char *p = cmd;\n\n    while (*p && argc < max_argc) {\n        /* 跳过前导空格 */\n        while (*p == \' \' || *p == \'\\t\' || *p == \'\\n\' || *p == \'\\r\') p++;\n        if (!*p) break;\n\n        argv[argc++] = (char *)p;\n\n        if (*p == \'\\\'\' || *p == \'"\') {\n            /* 引号包裹的参数：找匹配的结束引号 */\n            char quote = *p;\n            p++;\n            while (*p && *p != quote) p++;\n            if (*p) {\n                *p = \'\\0\';    /* 替换结束引号为 NUL */\n                p++;\n            }\n        } else {\n            /* 无引号：遇到空格或 NUL 结束 */\n            while (*p && *p != \' \' && *p != \'\\t\' && *p != \'\\n\' && *p != \'\\r\')\n                p++;\n            if (*p) *p++ = \'\\0\';\n        }\n    }\n    return argc;\n}\n```\n\n**要点**：\n\n1. **引号的处理方式**：找到匹配的结束引号后，**直接把引号替换成 `\\0`**，这样 `argv[i]` 就是一个合法的 C 字符串。不需要分配新内存，不需要拷贝。\n\n2. **只处理引号内的内容**。单引号和双引号在解析逻辑上是一样的（不像 bash 中双引号支持变量展开）。如果需要支持 bash 语义，需要额外的解析逻辑。\n\n3. **未闭合的引号处理**。当前实现中，如果引号没有闭合，`*p` 为 `\\0` 时退出循环，引号前的所有内容作为一个参数返回。这是合理的容错行为。\n\n4. **argv 数组由调用者提供**。这样不需要 `malloc`，适合嵌入式环境。调用者需要保证 `argv` 有足够的空间（比如 `char *argv[64]`）。\n\n**使用示例**：\n```c\nchar buf[] = "sensor start \"mode auto\" \'debug on\'";\nchar *argv[64];\nint argc = parse_args(buf, argv, 64);\n/* argc == 4, argv[0]="sensor", argv[1]="start", argv[2]="mode auto", argv[3]="debug on" */\n```\n\n**嵌入式实际使用场景**：\n\n- **串口命令解析**：终端用户通过串口输入 `sensor start "mode auto"`，解析后执行对应动作\n- **AT 命令解析**：`AT+CMD=param1 "param with space" param3`\n- **配置文件解析**：INI 文件中带引号的值\n- **你的 ECU 诊断系统**中，PC 端发出的 UDS 诊断命令如果是文本格式的，也可以用类似的解析器\n\n**面试追问方向**：\n- **怎么处理转义字符（如 `\"he said \\\\\\\"hi\\\\\\\"\"`）**？需要额外的状态机处理 `\\` 转义\n- **怎么处理 `=` 赋值（如 `param=value`）**？在解析出参数后，再按 `=` 拆分\n- **如果参数中包含空格但没有引号怎么办**？当前实现中空格就是分隔符，无法表示带空格的参数。这是 shell 解析器的一个经典设计选择\n\n**扩展**：如果要做完整的 shell 解析器，还需要处理：\n- `\\` 转义\n- `$variable` 展开\n- `$(command)` 子命令\n- `&&`、`||`、`|` 管道\n- 重定向 `>`、`<`\n\n但这些在嵌入式中很少需要，通常只需要基本的引号处理就够了。',
  followup: ['怎么处理转义字符？', '如何支持 = 赋值？', '为什么嵌入式中不需要完整的 shell 解析器？']
},
{
  id: 'code-030', cat: 'coding', type: 'single', level: 2, tags: ['sizeof', '结构体', '对齐'],
  q: '以下代码在 64 位 GCC 平台（默认对齐）上输出什么？\n\n```c\nstruct S {\n    char    a;\n    int     b;\n    short   c;\n    char    d;\n    double  e;\n    float   f;\n    int     g;\n};\nprintf("%zu\\n", sizeof(struct S));\n```',
  options: ['28', '32', '36', '40'],
  answer: [2],
  a: '**输出 32。**\n\n**按成员逐个分析内存布局**（64 位平台，`sizeof(long) = 8`，默认对齐值为 8）：\n\n| 成员 | 类型 | 大小 | 对齐要求 | 起始偏移 | 结束偏移 | 填充字节 |\n|---|---|---|---|---|---|---|\n| a | char | 1 | 1 | 0 | 1 | 0 |\n| 填充 | — | — | — | 1 | 4 | 3 字节 |\n| b | int | 4 | 4 | 4 | 8 | 0 |\n| c | short | 2 | 2 | 8 | 10 | 0 |\n| d | char | 1 | 1 | 10 | 11 | 0 |\n| 填充 | — | — | — | 11 | 16 | 5 字节 |\n| e | double | 8 | 8 | 16 | 24 | 0 |\n| f | float | 4 | 4 | 24 | 28 | 0 |\n| g | int | 4 | 4 | 28 | 32 | 0 |\n\n**总计：32 字节**\n\n**关键规则**：\n\n1. **每个成员的起始地址必须是其对齐要求的倍数**。`int` 对齐 4，所以 `b` 不能在偏移 1 处（1 不是 4 的倍数），必须等到偏移 4。\n\n2. **结构体的对齐要求 = 所有成员中最大的对齐要求**。这里 `double` 的对齐是 8，所以结构体的对齐也是 8。\n\n3. **结构体的总大小必须是其对齐要求的倍数**。即使 `f` 和 `g` 占了 24~32 共 8 字节，但因为结构体对齐是 8，所以总大小必须是 8 的倍数。32 刚好是 8 的倍数，不需要尾部填充。\n\n4. **编译器可以自由安排成员顺序**。如果把 `char` 类型的成员放在一起，可以节省空间。比如把 `a` 和 `d` 放在一起，`c` 紧跟在后面，总大小可以降到 28 字节。\n\n**为什么不是其他选项**：\n\n- **28**：如果编译器不填充尾部（不对齐到结构体对齐要求），或者把成员重新排序，可能得到 28。但 GCC 默认会对齐。\n- **36**：如果 `double` 的对错位置（比如放在 `d` 后面而不是 `c` 后面），需要更多的填充，可能到 36。\n- **40**：如果编译器使用更严格的对齐（比如 `#pragma pack(16)`），或者把 `float` 也按 8 对齐。\n\n**面试延伸**：\n\n1. **如何减少结构体的内存占用**？按对齐要求从大到小排列成员：\n```c\nstruct S_optimized {\n    double  e;    /* 8 字节对齐 */\n    int     b, g;\n    float   f;\n    short   c;\n    char    a, d;\n};\n/* sizeof = 28，节省了 4 字节 */\n```\n\n2. **#pragma pack(n)** 可以强制对齐到 n 字节，但会导致非对齐访问（在某些架构上性能下降或触发 Fault）。\n\n3. **位域的影响**：如果结构体中有位域，位域的打包规则是实现定义的（不同编译器可能不同），sizeof 的结果也会不同。所以在跨平台代码中应尽量避免依赖位域的布局。\n\n4. **`sizeof(struct S)` 在运行时是常量**。编译器在编译期就确定了，sizeof 不产生任何运行时开销。',
  followup: ['怎么按大小重新排列成员以节省空间？', '#pragma pack 会影响对齐吗？', '位域对 sizeof 的影响是什么？']
},
]);
