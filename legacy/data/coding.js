QBANK.add([
{
  id: 'code-001', cat: 'coding', type: 'qa', level: 2, tags: ['字符串', 'strcpy'],
  q: '手写实现 strcpy。写完说说你考虑了哪些细节。',
  a: '```c\nchar *my_strcpy(char *dst, const char *src)\n{\n    assert(dst != NULL && src != NULL);      /* 1. 参数校验 */\n    char *ret = dst;                          /* 2. 保存原始指针用于返回 */\n    while ((*dst++ = *src++) != \'\\0\')         /* 3. 拷贝并自动带上结束符 */\n        ;\n    return ret;                               /* 4. 返回 dst 支持链式调用 */\n}\n```\n\n**面试官在这道题里想看的四个点**：\n\n1. **`src` 要加 `const`**。表明函数不修改源数据，编译器还能帮忙检查。**不加 const 是最常见的失分点。**\n\n2. **参数判空**。生产代码里更常见的是返回错误码而不是 assert，因为 assert 在 Release 版本会被编译掉。\n\n3. **返回值是 `char *` 而不是 `void`**。返回目标地址是为了支持 `strlen(strcpy(dst, src))` 这样的链式调用。**必须先保存 dst 的原值**，因为循环里它被修改了。\n\n4. **循环条件的写法**。`while ((*dst++ = *src++) != \'\\0\')` 一行完成了"拷贝 + 判断 + 移动指针"，而且**结束符本身也被拷贝了**（先赋值再判断）。写成 `while (*src) { *dst++ = *src++; }` 就会漏掉结尾的 `\\0`，必须在循环后补一句 `*dst = \'\\0\'`。\n\n**必须主动指出的安全问题**：\n\n**strcpy 本身是不安全的**——它不知道 dst 有多大，src 太长就会缓冲区溢出。所以：\n- 实际项目里应该用 `snprintf(dst, sizeof(dst), \"%s\", src)` 或 `strlcpy`\n- **`strncpy` 不是安全的替代品**：源串太长时它不补 `\\0`，源串太短时它会用 `\\0` 填满整个缓冲区（浪费性能）\n\n主动说出这一点，比单纯写对代码更能加分——它说明你不只是会背实现，而是知道什么时候不该用它。\n\n**可能的追问**：如果 dst 和 src 内存重叠会怎样？（行为未定义，需要用 memmove 的思路处理方向）',
  followup: ['为什么 strncpy 不是"安全版 strcpy"？', 'dst 和 src 重叠时会发生什么？', '怎么写一个带长度限制且保证结尾的版本？']
},
{
  id: 'code-002', cat: 'coding', type: 'qa', level: 2, tags: ['memcpy'],
  q: '手写实现 memcpy，并说明怎么处理内存重叠。',
  a: '**基础版本**：\n```c\nvoid *my_memcpy(void *dst, const void *src, size_t n)\n{\n    assert(dst != NULL && src != NULL);\n    char *d = (char *)dst;\n    const char *s = (const char *)src;\n    while (n--)\n        *d++ = *s++;\n    return dst;\n}\n```\n\n**处理重叠的版本（这就是 memmove）**：\n```c\nvoid *my_memmove(void *dst, const void *src, size_t n)\n{\n    char *d = (char *)dst;\n    const char *s = (const char *)src;\n\n    if (d < s || d >= s + n) {\n        /* 不重叠，或 dst 在 src 前面：正向拷贝 */\n        while (n--) *d++ = *s++;\n    } else {\n        /* dst 落在 src 内部靠后：从后往前拷，避免覆盖还没读的数据 */\n        d += n;\n        s += n;\n        while (n--) *--d = *--s;\n    }\n    return dst;\n}\n```\n\n**关键点**：\n\n1. **参数类型是 `void *`**，必须转成 `char *` 才能做指针算术（`void *` 不能解引用也不能 ++）。\n\n2. **`src` 加 `const`**。\n\n3. **重叠的判断方向**：只有当 `dst > src` 且区域重叠时才需要反向拷贝。因为正向拷贝时，`dst` 在前面写会覆盖 `src` 后面还没读的部分。`dst < src` 时正向拷贝是安全的。\n\n4. **memcpy 不保证处理重叠**，这是标准明确的。所以标准库里才有两个函数——memcpy 因为不用判断方向而更快，用在确定不重叠的场景。\n\n**性能优化（能说出来是加分项）**：\n逐字节拷贝在 32 位机上很慢。实际的库实现会：\n```c\n/* 先按字（4 字节）拷贝，最后处理尾部不足一个字的部分 */\nwhile (n >= 4 && ((uintptr_t)d & 3) == 0 && ((uintptr_t)s & 3) == 0) {\n    *(uint32_t *)d = *(uint32_t *)s;\n    d += 4; s += 4; n -= 4;\n}\nwhile (n--) *d++ = *s++;\n```\n**前提是地址对齐**——ARM 上非对齐的字访问可能触发 Fault。所以要先判断对齐，不对齐时先逐字节拷到对齐边界。\n\n真实的 libc 实现还会用 SIMD 指令、循环展开、以及针对不同长度的特化路径。**嵌入式上更实际的选择是用 DMA 搬运大块数据**，完全不占 CPU。',
  followup: ['为什么非对齐的字访问在 ARM 上有问题？', '什么时候该用 DMA 代替 memcpy？', 'memcpy 拷贝结构体有什么隐患？']
},
{
  id: 'code-003', cat: 'coding', type: 'qa', level: 2, tags: ['字符串', 'atoi'],
  q: '手写实现 atoi（字符串转整数），考虑各种边界情况。',
  a: '```c\nint my_atoi(const char *s)\n{\n    if (s == NULL) return 0;\n\n    /* 1. 跳过前导空白 */\n    while (*s == \' \' || (*s >= \'\\t\' && *s <= \'\\r\')) s++;\n\n    /* 2. 处理符号 */\n    int sign = 1;\n    if (*s == \'+\' || *s == \'-\') {\n        if (*s == \'-\') sign = -1;\n        s++;\n    }\n\n    /* 3. 逐位累加，同时检测溢出 */\n    long long result = 0;\n    while (*s >= \'0\' && *s <= \'9\') {\n        result = result * 10 + (*s - \'0\');\n        if (sign == 1 && result > INT_MAX)  return INT_MAX;   /* 4. 溢出钳位 */\n        if (sign == -1 && -result < INT_MIN) return INT_MIN;\n        s++;\n    }\n\n    return (int)(sign * result);\n}\n```\n\n**这道题考的全是边界情况**，能想全就赢了：\n\n1. **空指针**\n2. **前导空白**（空格、\\t、\\n 等）要跳过\n3. **正负号**，且只能有一个\n4. **非数字字符处**停止（`\"123abc\"` → 123）\n5. **溢出**：`\"99999999999\"` 该返回什么？标准 atoi 的行为是未定义的，而 LeetCode 那道题要求钳位到 INT_MAX/INT_MIN。**面试时要主动问清楚要求**\n6. **INT_MIN 的不对称性**：`-2147483648` 的绝对值超过了 INT_MAX，用 int 中间变量会溢出。所以要用 `long long`，或者**全程用负数累加**（负数范围更大一位）\n7. 完全没有数字（`\"abc\"`）→ 返回 0\n\n**用负数累加的技巧（避免 long long）**：\n```c\nint result = 0;\nwhile (isdigit(*s)) {\n    int digit = *s - \'0\';\n    if (result < INT_MIN / 10 || (result == INT_MIN / 10 && digit > 8))\n        return sign == 1 ? INT_MAX : INT_MIN;\n    result = result * 10 - digit;      /* 累加成负数 */\n    s++;\n}\nreturn sign == 1 ? -result : result;\n```\n这个写法在 32 位 MCU 上更实用，因为**避免了 64 位运算**（无硬件 64 位乘除时开销明显）。\n\n**嵌入式的实际考量**：解析协议里的数字字段时，通常不用 atoi 而是自己写——因为需要明确的错误返回（区分"转换失败"和"结果是 0"），而 atoi 做不到。更好的是用 `strtol`，它通过 `endptr` 告诉你解析停在哪里，并用 errno 报告溢出。',
  followup: ['为什么 strtol 比 atoi 更适合生产代码？', 'INT_MIN 取绝对值为什么会溢出？', '怎么写一个能报告错误的版本？']
},
{
  id: 'code-004', cat: 'coding', type: 'qa', level: 2, tags: ['链表'],
  q: '手写反转单链表，迭代和递归两种写法。',
  a: '**迭代版（推荐，O(1) 空间）**：\n```c\ntypedef struct node { int val; struct node *next; } node_t;\n\nnode_t *reverse(node_t *head)\n{\n    node_t *prev = NULL;\n    node_t *cur  = head;\n    while (cur) {\n        node_t *next = cur->next;   /* 先存下一个，否则改完就找不到了 */\n        cur->next = prev;           /* 反转指向 */\n        prev = cur;                 /* prev 前进 */\n        cur = next;                 /* cur 前进 */\n    }\n    return prev;                    /* 循环结束时 prev 是新的头 */\n}\n```\n\n**递归版**：\n```c\nnode_t *reverse_rec(node_t *head)\n{\n    if (head == NULL || head->next == NULL)\n        return head;                    /* 空表或只剩一个节点，它就是新头 */\n\n    node_t *new_head = reverse_rec(head->next);\n    head->next->next = head;            /* 让下一个节点指回自己 */\n    head->next = NULL;                  /* 断开原来的指向，否则成环 */\n    return new_head;\n}\n```\n\n**要点**：\n\n1. **迭代版的三个指针缺一不可**。`next` 必须在修改 `cur->next` **之前**保存，这是最容易写错的地方。\n\n2. **返回值是 `prev` 而不是 `cur`**。循环结束时 `cur == NULL`，`prev` 指向最后一个处理过的节点，也就是新的头。\n\n3. **递归版的 `head->next = NULL` 不能漏**。不写的话最后两个节点会互相指向，形成环，遍历时死循环。\n\n4. **空表和单节点要能正确处理**——两种写法都自然覆盖了。\n\n**嵌入式的选择：一定用迭代版。**\n- 递归版的**栈深度等于链表长度**，链表长了必然栈溢出。MCU 的栈本来就只有几 KB\n- **MISRA C 直接禁止递归**\n- 迭代版空间 O(1)，时间也一样是 O(n)\n\n面试时可以两种都写，但**要主动说明"实际项目里我会用迭代版，因为递归的栈深度不可控"**——这个判断比会写递归更有价值。\n\n**变体题**：反转链表的前 k 个节点、每 k 个一组反转、判断回文链表（反转后半部分再比较）。这些都是在这道题基础上加条件。',
  followup: ['为什么 MISRA C 禁止递归？', '怎么反转链表中间的一段？', '怎么判断一个链表是回文的？']
},
{
  id: 'code-005', cat: 'coding', type: 'qa', level: 2, tags: ['字节序'],
  q: '写一个判断大小端的函数，再写一个 32 位整数的字节序转换函数。',
  a: '**判断字节序**：\n```c\n/* 方法一：union */\nint is_little_endian(void)\n{\n    union { uint16_t v; uint8_t b[2]; } u = { .v = 0x0102 };\n    return u.b[0] == 0x02;      /* 低地址放低位字节 → 小端 */\n}\n\n/* 方法二：指针强转 */\nint is_little_endian2(void)\n{\n    uint16_t v = 0x0102;\n    return *(uint8_t *)&v == 0x02;\n}\n```\n\n**32 位字节序转换**：\n```c\nuint32_t bswap32(uint32_t x)\n{\n    return ((x & 0x000000FFu) << 24) |\n           ((x & 0x0000FF00u) <<  8) |\n           ((x & 0x00FF0000u) >>  8) |\n           ((x & 0xFF000000u) >> 24);\n}\n```\n\n**16 位版本**：\n```c\nuint16_t bswap16(uint16_t x)\n{\n    return (uint16_t)((x << 8) | (x >> 8));\n}\n```\n\n**要点**：\n\n1. **字面量加 `u` 后缀**。`0xFF000000` 在 32 位 int 上是负数，参与运算可能出意外。用 unsigned 更安全。\n\n2. **移位前先掩码**。虽然本例中先移位再掩码也对，但先掩码的写法意图更清晰，也避免了有符号右移的算术填充问题。\n\n3. **注意 `x >> 24` 后不需要再掩码**（高位移出去了），但为了对称和可读性写上也无妨。\n\n**更好的实践（能说出来加分）**：\n- **优先用编译器内建函数**：`__builtin_bswap32()`。很多架构（包括 ARM）有**单条字节交换指令 REV**，内建函数会直接生成它，比上面的四次移位快得多\n- 网络编程用标准的 `htonl/ntohl`，它们在大端机器上是空操作\n- **不要在协议解析里用结构体强转 + bswap**——对齐和填充的问题依然存在。**逐字节手工组装才是跨平台的正道**：\n```c\nuint32_t get_be32(const uint8_t *p) {\n    return ((uint32_t)p[0] << 24) | ((uint32_t)p[1] << 16) |\n           ((uint32_t)p[2] <<  8) |  (uint32_t)p[3];\n}\n```\n**这个函数不管在什么字节序的平台上，读出来的都是同一个值**——这才是协议解析该用的写法，连字节序判断都不需要。\n\n**为什么这题常考**：它同时考了字节序概念、位运算、类型安全，而且和实际的协议开发直接相关。',
  followup: ['为什么 get_be32 这种写法不需要判断字节序？', '__builtin_bswap32 在 ARM 上生成什么指令？', '结构体强转解析协议有哪些风险？']
},
{
  id: 'code-006', cat: 'coding', type: 'qa', level: 2, tags: ['位运算'],
  q: '写出下面几个位运算函数：统计 1 的个数、判断 2 的幂、交换两个数不用临时变量、取最低位的 1。',
  a: '**1. 统计二进制中 1 的个数**\n```c\n/* Brian Kernighan 算法：循环次数 = 1 的个数 */\nint count_ones(uint32_t x)\n{\n    int n = 0;\n    while (x) { x &= (x - 1); n++; }    /* 每次消掉最低位的 1 */\n    return n;\n}\n```\n`x - 1` 把最低位的 1 变 0、右边的 0 全变 1，相与正好抹掉那个 1。\n\n**2. 判断是否为 2 的整数次幂**\n```c\nint is_power_of_two(uint32_t x)\n{\n    return x && !(x & (x - 1));         /* 注意 x != 0 的前提 */\n}\n```\n2 的幂在二进制里只有一个 1，`x & (x-1)` 必为 0。**`x &&` 不能少**，否则 0 会被误判为真。\n\n**3. 交换两个数（不用临时变量）**\n```c\nvoid swap_xor(int *a, int *b)\n{\n    if (a == b) return;                 /* 关键：必须判断同一地址！ */\n    *a ^= *b;\n    *b ^= *a;\n    *a ^= *b;\n}\n```\n**这道题的陷阱就在 `a == b` 的判断上。** 如果两个指针指向同一个变量，三次异或的结果是**把它清零**。这是面试官最想看你有没有考虑到的点。\n\n**但实际项目里应该直接用临时变量**：\n```c\nvoid swap(int *a, int *b) { int t = *a; *a = *b; *b = t; }\n```\n可读性好，而且**在现代编译器上性能一样甚至更好**（寄存器分配比三次内存读写更优）。异或交换是个面试题技巧，不是工程实践。\n\n**4. 取最低位的 1**\n```c\nuint32_t lowbit(uint32_t x) { return x & (-x); }\n```\n`-x` 是补码取反加一，正好让最低位的 1 保持不变、其余位与原数互补。`12 (1100) & -12 (0100) = 4 (0100)`。\n\n**5. 其他常用技巧**\n```c\nx & (x - 1)            /* 清掉最低位的 1 */\nx | (x + 1)            /* 把最低位的 0 置 1 */\n31 - __builtin_clz(x)  /* 最高位 1 的位置（ARM 的 CLZ 是单周期指令）*/\n(x ^ y) < 0            /* 判断 x 和 y 符号是否相反（有符号数）*/\n```\n\n**面试时的加分表述**：主动提"实际用 `__builtin_popcount()`，很多架构有单条指令"和"异或交换是技巧不是好实践"，说明你分得清面试题和工程代码。',
  followup: ['为什么异或交换在同一地址时会清零？', '__builtin_popcount 在没有硬件指令的平台上怎么实现？', 'CLZ 指令在 RTOS 调度里有什么用？']
},
{
  id: 'code-007', cat: 'coding', type: 'qa', level: 2, tags: ['环形缓冲'], resume: true,
  q: '现场写一个单生产者单消费者的环形缓冲区，用于串口中断和主循环之间传数据。',
  a: '```c\n#define RB_SIZE 256                       /* 必须是 2 的幂 */\n\ntypedef struct {\n    uint8_t  buf[RB_SIZE];\n    volatile uint16_t head;               /* 只由生产者(ISR)写 */\n    volatile uint16_t tail;               /* 只由消费者(主循环)写 */\n} ring_t;\n\nstatic inline int rb_empty(const ring_t *r) { return r->head == r->tail; }\n\nstatic inline int rb_full(const ring_t *r)\n{\n    return ((r->head + 1) & (RB_SIZE - 1)) == r->tail;\n}\n\n/* 生产者：中断里调用 */\nint rb_put(ring_t *r, uint8_t d)\n{\n    uint16_t next = (r->head + 1) & (RB_SIZE - 1);\n    if (next == r->tail) return -1;       /* 满，丢弃并返回失败 */\n    r->buf[r->head] = d;                  /* 先写数据 */\n    r->head = next;                       /* 后更新索引 —— 顺序不能反 */\n    return 0;\n}\n\n/* 消费者：主循环里调用 */\nint rb_get(ring_t *r, uint8_t *d)\n{\n    if (r->head == r->tail) return -1;    /* 空 */\n    *d = r->buf[r->tail];                 /* 先读数据 */\n    r->tail = (r->tail + 1) & (RB_SIZE - 1);  /* 后更新索引 */\n    return 0;\n}\n```\n\n**写完之后要主动讲清楚四个设计点**（这才是这道题的价值）：\n\n**1. 为什么不用加锁**\n每个索引只有一个写者：生产者只写 `head`，消费者只写 `tail`，双方只读对方的索引。**不存在两个执行流同时修改同一变量，也就没有读-改-写竞态**。所以中断和主循环之间不需要关中断保护。\n\n**2. 为什么顺序不能反**\n必须**先写数据、再更新索引**。反过来的话，消费者可能看到 `head` 已经前进但那个位置的数据还没写进去，读到脏数据。同理消费者要先读数据再更新 `tail`。\n\n**3. 为什么索引要 `volatile`**\n防止编译器把索引缓存在寄存器里——中断修改了内存，主循环还在用寄存器里的旧值。\n\n**4. 为什么容量取 2 的幂**\n回绕用 `& (SIZE-1)` 代替 `% SIZE`。**很多 MCU 没有硬件除法器，取模要几十个周期，位与只要一个**。\n\n**5. 为什么牺牲一个存储单元**\n用 `head == tail` 表示空、`(head+1) == tail` 表示满，代价是实际容量只有 `SIZE-1`。这样换来了免锁。想用满容量可以用 Linux kfifo 的做法（索引不取模，靠无符号溢出）。\n\n**可能的追问**：\n- **多生产者怎么办**？必须加锁或用 CAS，免锁性质不再成立\n- **多核弱内存序平台**？还需要在写数据和更新索引之间加**内存屏障**（`__DMB()`），编译器屏障和 volatile 不足以保证 CPU 层面的顺序\n- **满了应该丢新的还是丢旧的**？取决于业务——日志场景丢旧的（保新），控制指令场景丢新的（保序），无论哪种都要**统计丢弃次数**',
  followup: ['多核平台上还需要加什么？', '缓冲满了应该丢新数据还是旧数据？', 'kfifo 用无符号溢出的方案好在哪？']
},
{
  id: 'code-008', cat: 'coding', type: 'qa', level: 2, tags: ['协议', '状态机'], resume: true,
  q: '现场写一个串口协议帧解析器，帧格式是 [0xAA][长度][数据...][校验和]。',
  a: '```c\n#define MAX_PAYLOAD 64\n\ntypedef enum { S_HEAD, S_LEN, S_DATA, S_SUM } state_t;\n\ntypedef struct {\n    state_t st;\n    uint8_t buf[MAX_PAYLOAD];\n    uint8_t len;\n    uint8_t idx;\n} parser_t;\n\nvoid parser_init(parser_t *p) { p->st = S_HEAD; }\n\n/* 每收到一个字节调用一次；解析出完整帧时调用 on_frame */\nvoid parser_feed(parser_t *p, uint8_t b)\n{\n    switch (p->st) {\n    case S_HEAD:\n        if (b == 0xAA) p->st = S_LEN;\n        break;\n\n    case S_LEN:\n        if (b == 0 || b > MAX_PAYLOAD) {   /* 长度非法 —— 关键的边界检查 */\n            p->st = S_HEAD;                /* 重新同步 */\n            break;\n        }\n        p->len = b;\n        p->idx = 0;\n        p->st  = S_DATA;\n        break;\n\n    case S_DATA:\n        p->buf[p->idx++] = b;\n        if (p->idx >= p->len) p->st = S_SUM;\n        break;\n\n    case S_SUM: {\n        uint8_t sum = p->len;\n        for (uint8_t i = 0; i < p->len; i++) sum += p->buf[i];\n        if (sum == b) on_frame(p->buf, p->len);   /* 校验通过 */\n        p->st = S_HEAD;                            /* 无论对错都回到等帧头 */\n        break;\n    }\n    }\n}\n```\n\n**这道题真正考的是边界处理，不是状态机本身**。写完要主动讲这几点：\n\n**1. 长度字段必须校验上限**\n`if (b > MAX_PAYLOAD)` 这一句不能少。**外部输入的长度字段是攻击者可控的**，不检查就是缓冲区溢出漏洞。这是最重要的一行。\n\n**2. 任何异常都要能重新同步**\n长度非法、校验失败——都回到 `S_HEAD` 重新找帧头，而不是卡死或继续用错误数据。\n\n**3. 必须有帧间超时**\n上面的代码还缺一个：如果收到一半就没有后续字节了（对方断电），状态机会永远停在 `S_DATA`。**下一帧的数据会被当成这一帧的续接，从此永久错位**。\n```c\nvoid parser_tick(parser_t *p) {          /* 周期调用，比如 10ms */\n    if (p->st != S_HEAD && ++p->timeout > TIMEOUT_TICKS) {\n        p->st = S_HEAD;\n        p->timeout = 0;\n    }\n}\n```\n**主动补上这一条，是这道题最大的加分点。**\n\n**4. 状态放在结构体里而不是 static**\n这样解析器是可重入的，能同时解析多路串口。\n\n**5. 不能假设一次 read 收到一整帧**\n这正是要用状态机而不是"读一个 buffer 然后解析"的原因。串口是字节流，一帧可能分几次到达，也可能一次到达好几帧。\n\n**可以主动提的改进**：\n- **数据区出现 0xAA 怎么办**？当前设计靠长度字段和校验和兜底，出错时重新同步可能会错过真正的帧头。更健壮的做法是**转义**（如 SLIP 的字节填充）或者把已缓存的字节重新走一遍状态机\n- 校验和太弱，重要数据应该用 **CRC16**\n- 这个状态机**不依赖任何硬件，可以在 PC 上做单元测试**，喂各种畸形数据验证鲁棒性',
  followup: ['数据区里出现帧头字节 0xAA 怎么处理？', '怎么给这个解析器写单元测试？', '为什么帧间超时这么重要？']
},
{
  id: 'code-009', cat: 'coding', type: 'single', level: 2, tags: ['输出题'],
  q: '下面代码在 32 位小端平台上输出什么？\n\n```c\nunion U {\n    int  i;\n    char c[4];\n};\nunion U u;\nu.i = 0x12345678;\nprintf("%x", u.c[0]);\n```',
  options: ['12', '78', '34', '56'],
  answer: [1],
  a: '**输出 78。**\n\n小端序：**低位字节存放在低地址**。`0x12345678` 在内存中的排列是：\n```\n地址:    低 ──────────────→ 高\n        c[0]  c[1]  c[2]  c[3]\n        0x78  0x56  0x34  0x12\n```\n所以 `u.c[0]` 是最低位字节 `0x78`。\n\n**大端平台上则是 `0x12`**（高位字节在低地址）。\n\n**union 的关键性质**：所有成员**共用同一块内存**，起始地址相同，大小等于最大成员（再按对齐取整）。这里 `sizeof(union U)` 是 4。\n\n**这题在实际项目里的意义**：\n\n1. **这正是判断字节序的经典写法**：\n```c\nint is_little_endian(void) {\n    union { uint16_t v; uint8_t b[2]; } u = { .v = 0x0102 };\n    return u.b[0] == 0x02;\n}\n```\n\n2. **union 常用于协议报文和字节流的互转**：\n```c\nunion { msg_t msg; uint8_t raw[sizeof(msg_t)]; } frame;\n```\n收到裸字节填进 `raw`，直接按 `msg` 解析。**但这样做依赖平台的字节序和结构体布局，跨平台不可靠**——正确的做法还是逐字节手工组装。\n\n3. **寄存器的位域访问**：\n```c\ntypedef union {\n    uint32_t all;\n    struct { uint32_t en:1; uint32_t mode:3; uint32_t rsv:28; } bits;\n} ctrl_t;\n```\n既能整体读写又能按位操作。**但位域的位序是实现定义的**，跨编译器不保证一致。\n\n**一个容易被追问的点**：`char` 的符号性是实现定义的。如果 `u.c[0]` 是 `signed char` 且值大于 0x7F，用 `%x` 打印时会因为整型提升带来符号扩展，输出 `ffffff78` 这样的结果。**协议解析一律用 `uint8_t`**，不要用 `char`。',
  followup: ['为什么协议解析要用 uint8_t 而不是 char？', '位域的位序为什么不能跨平台依赖？', '结构体的填充字节在 union 里怎么表现？']
},
{
  id: 'code-010', cat: 'coding', type: 'qa', level: 2, tags: ['宏'],
  q: '写几个嵌入式常用的宏：数组元素个数、最大值、位操作、结构体成员偏移。',
  a: '**1. 数组元素个数**\n```c\n#define ARRAY_SIZE(a)   (sizeof(a) / sizeof((a)[0]))\n```\n**陷阱：对指针会给出错误结果且不报错**。`int *p; ARRAY_SIZE(p)` 得到 `4/4 = 1`。Linux 内核用了一个技巧在编译期拦住这种误用：\n```c\n#define ARRAY_SIZE(a)  (sizeof(a) / sizeof((a)[0]) + \\\n                        sizeof(typeof(int[1 - 2 * !!__builtin_types_compatible_p(typeof(a), typeof(&a[0]))])) * 0)\n```\n如果 `a` 是指针，`__builtin_types_compatible_p` 为真，数组大小变成 `1-2 = -1`，**编译报错**。\n\n**2. 取最大值（避免参数多次求值）**\n```c\n/* 朴素版：MAX(i++, j) 会让 i 自增两次 */\n#define MAX(a, b)  ((a) > (b) ? (a) : (b))\n\n/* GCC 语句表达式版：参数只求值一次 */\n#define MAX_SAFE(a, b) ({ \\\n    typeof(a) _a = (a);   \\\n    typeof(b) _b = (b);   \\\n    _a > _b ? _a : _b; })\n```\n\n**3. 位操作**\n```c\n#define BIT(n)          (1UL << (n))\n#define SET_BIT(R, n)   ((R) |=  BIT(n))\n#define CLR_BIT(R, n)   ((R) &= ~BIT(n))\n#define TOG_BIT(R, n)   ((R) ^=  BIT(n))\n#define GET_BIT(R, n)   (((R) >> (n)) & 1UL)\n```\n**用 `1UL` 而不是 `1`**：`1 << 31` 在 32 位 int 上是有符号溢出（UB）。\n\n**4. 结构体成员偏移与反查**\n```c\n#define offsetof(TYPE, MEMBER) ((size_t)&((TYPE *)0)->MEMBER)\n\n#define container_of(ptr, type, member) ({          \\\n    const typeof(((type *)0)->member) *__mptr = (ptr); \\\n    (type *)((char *)__mptr - offsetof(type, member)); })\n```\n\n**5. 编译期断言**\n```c\n#define STATIC_ASSERT(cond, msg)  typedef char static_assert_##msg[(cond) ? 1 : -1]\n/* C11 之后直接用 _Static_assert 或 C++ 的 static_assert */\nSTATIC_ASSERT(sizeof(frame_t) == 8, frame_size);\n```\n**协议结构体的大小校验特别值得加**——加个成员导致布局变化，编译时就能发现。\n\n**写宏的三条铁律**：\n1. **每个参数都加括号，整体也加括号**。`#define ADD(a,b) a+b` 遇到 `ADD(1,2)*3` 会变成 `1+2*3 = 7`\n2. **多语句宏用 `do { ... } while(0)` 包裹**，保证 `if (x) MACRO(); else ...` 语法正确\n3. **参数只求值一次**，否则 `MACRO(i++)` 会出事\n\n**更根本的建议**：**能用 `static inline` 函数就不要用宏**——有类型检查、能调试、参数只求值一次。宏只留给需要操作类型本身、符号拼接（`##`）、或条件编译的场景。',
  followup: ['do-while(0) 包裹宏解决什么问题？', '为什么 Linux 的 ARRAY_SIZE 要那么复杂？', 'static_assert 在嵌入式里还有哪些用途？']
},
{
  id: 'code-011', cat: 'coding', type: 'qa', level: 2, tags: ['排序', '滤波'],
  q: '写一个中值滤波函数：从 N 个采样值中取中位数。',
  a: '```c\n#define N 5    /* 取奇数 */\n\nuint16_t median_filter(uint16_t *data, int n)\n{\n    /* 插入排序：n 很小时最优 —— 代码短、无递归、近乎有序时极快 */\n    for (int i = 1; i < n; i++) {\n        uint16_t key = data[i];\n        int j = i - 1;\n        while (j >= 0 && data[j] > key) {\n            data[j + 1] = data[j];\n            j--;\n        }\n        data[j + 1] = key;\n    }\n    return data[n / 2];\n}\n```\n\n**要点**：\n\n1. **为什么用插入排序而不是快排**\n   - n 通常只有 3、5、7，**小数据量下插入排序的常数因子最小**\n   - 代码只有十行，Flash 占用最小\n   - **无递归**，栈占用确定——这对 MCU 很重要\n   - 稳定、原地\n   直接答"用 qsort" 是不合适的：标准库的 qsort 有函数指针调用开销，代码体积也大得多。\n\n2. **注意这个实现会修改输入数组**。如果原始数据还要用，要先拷贝一份。面试时应该主动说明或者写成不修改原数组的版本。\n\n3. **n 取奇数**，这样中位数是唯一的第 `n/2` 个元素。偶数时需要定义是取两个中间值的平均还是取其中一个。\n\n**中值滤波的价值**：\n**对脉冲型干扰极其有效**——一两个离群点完全不影响中位数。这是它相比算术平均的核心优势：平均值会被一个极端值严重污染，中位数不会。\n\n**实际项目里更常用的是"中位值平均滤波"**：\n```c\nuint16_t median_average(uint16_t *data, int n)\n{\n    insertion_sort(data, n);\n    uint32_t sum = 0;\n    for (int i = 1; i < n - 1; i++)      /* 去掉一个最大、一个最小 */\n        sum += data[i];\n    return (uint16_t)(sum / (n - 2));\n}\n```\n**兼顾了中值的抗脉冲能力和平均的抗随机噪声能力**，是温度、压力这类慢变信号的工程首选。\n\n**可能的追问**：\n- **如果是滑动窗口（每来一个新值就更新一次）呢**？每次都排序是 O(n log n)，可以维护一个有序数组，新值插入、旧值删除都是 O(n)，或者用双堆维护中位数\n- **滤波会引入延迟**，控制系统里延迟影响稳定性，不能盲目加大窗口\n- **应该先排查干扰源**（电源、接地、采样时机避开 PWM 开关），硬件解决比软件滤波干净',
  followup: ['滑动窗口的中值怎么高效维护？', '中值滤波和均值滤波分别适合什么干扰？', '滤波延迟对控制系统有什么影响？']
},
{
  id: 'code-012', cat: 'coding', type: 'qa', level: 3, tags: ['内存池'],
  q: '写一个固定大小的内存池，要求申请和释放都是 O(1)。',
  a: '```c\ntypedef struct block { struct block *next; } block_t;\n\ntypedef struct {\n    block_t *free_list;\n    uint8_t *pool;\n    size_t   block_size;\n    size_t   count;\n    size_t   used;          /* 统计用 */\n    size_t   min_free;      /* 历史最低水位 */\n} pool_t;\n\nint pool_init(pool_t *p, void *mem, size_t block_size, size_t count)\n{\n    if (block_size < sizeof(block_t)) return -1;   /* 块必须放得下一个指针 */\n\n    p->pool = (uint8_t *)mem;\n    p->block_size = block_size;\n    p->count = count;\n    p->used = 0;\n    p->min_free = count;\n    p->free_list = NULL;\n\n    /* 把所有块串成空闲链表（借用块自身的空间存指针）*/\n    for (size_t i = 0; i < count; i++) {\n        block_t *b = (block_t *)(p->pool + i * block_size);\n        b->next = p->free_list;\n        p->free_list = b;\n    }\n    return 0;\n}\n\nvoid *pool_alloc(pool_t *p)                    /* O(1) */\n{\n    if (p->free_list == NULL) return NULL;\n    block_t *b = p->free_list;\n    p->free_list = b->next;\n    p->used++;\n    if (p->count - p->used < p->min_free)\n        p->min_free = p->count - p->used;\n    return b;\n}\n\nint pool_free(pool_t *p, void *ptr)            /* O(1) */\n{\n    /* 合法性检查：地址必须落在池内且按块对齐 */\n    uint8_t *q = (uint8_t *)ptr;\n    if (q < p->pool || q >= p->pool + p->count * p->block_size) return -1;\n    if ((size_t)(q - p->pool) % p->block_size != 0) return -1;\n\n    block_t *b = (block_t *)ptr;\n    b->next = p->free_list;\n    p->free_list = b;\n    p->used--;\n    return 0;\n}\n```\n\n**设计的关键点**：\n\n**1. 空闲块自身存链表指针**\n空闲块反正没被使用，**直接借它的前几个字节存 `next` 指针**，不需要任何额外的管理数据结构。所以块大小必须 ≥ `sizeof(void*)`。这是整个设计最巧妙的地方。\n\n**2. 为什么是 O(1)**\n申请就是摘链表头，释放就是插链表头，都是常数次指针操作。**时间完全确定**，这正是实时系统需要的——malloc 的耗时取决于堆的碎片状况，不可预测。\n\n**3. 不产生外部碎片**\n所有块等大，释放的块必然能被下次申请使用。**这是它相比 malloc 最本质的优势**——MCU 上没有 MMU，外部碎片是不可逆的。\n\n**4. 合法性检查**\n`pool_free` 里的两个检查能抓出"释放了不属于这个池的指针"和"释放了一个偏移错误的地址"。**再进一步可以在块头加魔术字，检测重复释放**。\n\n**5. 水位统计**\n`min_free` 记录历史最低剩余，长跑测试后看这个值判断池子开多大合适。**没有这个统计，你永远不知道池子是刚好够还是差点就爆了**。\n\n**并发保护**：链表头的操作要保护。中断和任务都可能申请释放时，需要关中断或用原子 CAS。**这是实现时最容易漏的地方**，面试时应该主动提。\n\n**实际应用**：FreeRTOS 的 heap_1、lwIP 的 pbuf 池、各种协议栈的报文缓冲，都是这个思路。',
  followup: ['怎么检测重复释放？', '中断和任务共用内存池怎么做并发保护？', '需要多种规格的块时怎么设计？']
},
{
  id: 'code-013', cat: 'coding', type: 'single', level: 2, tags: ['输出题', '指针'],
  q: '下面代码输出什么？\n\n```c\nvoid func(char *p) { p = malloc(10); }\n\nint main(void) {\n    char *str = NULL;\n    func(str);\n    if (str == NULL) printf("NULL");\n    else printf("OK");\n    return 0;\n}\n```',
  options: ['OK', 'NULL', '段错误', '编译错误'],
  answer: [1],
  a: '**输出 NULL，而且还泄漏了 10 字节内存。**\n\n**原因：C 是值传递。** `func(str)` 传进去的是 `str` 的**值的副本**。函数内的 `p` 是一个新的局部指针变量，`p = malloc(10)` 只是改了这个副本的值，**调用者的 `str` 完全没有变化**。\n\n函数返回后 `p` 消失，malloc 出来的那 10 字节**再也没有指针指向它**，成了泄漏。\n\n**正确写法一：传二级指针**\n```c\nvoid func(char **p) { *p = malloc(10); }\nfunc(&str);        /* 传 str 的地址 */\n```\n通过 `*p` 修改的才是调用者的 `str` 本身。\n\n**正确写法二：返回值（更清晰，推荐）**\n```c\nchar *func(void) { return malloc(10); }\nstr = func();\n```\n\n**牢记的规则**：**要在函数里修改调用者的某个变量，就必须传这个变量的地址。** 想改 `int` 就传 `int*`，想改 `char*` 就传 `char**`。\n\n**这题引申的实际问题**：\n\n1. **同样的错误也出现在 free 上**：\n```c\nvoid safe_free(void *p) { free(p); p = NULL; }   /* 调用者的指针没被置空！*/\nvoid safe_free(void **p) { free(*p); *p = NULL; } /* 正确 */\n```\n\n2. **数组作为参数会退化成指针**，函数内 `sizeof` 得到的是指针大小，拿不到数组长度。\n\n3. **嵌入式的更好做法：由调用者提供缓冲区**\n```c\nint func(char *buf, size_t len);    /* 内存归属清晰，无需动态分配 */\n```\n这样内存的所有权和生命周期都在调用者手里，既避免了这类错误，也避免了动态分配——**MCU 上应该优先用这个模式**。\n\n**面试价值**：这是最经典的指针题之一，答错说明对"值传递"的理解不牢。答对之后主动说出"实际项目里我会让调用者传缓冲区"，能体现工程判断。',
  followup: ['free 后置 NULL 的正确写法是什么？', '为什么嵌入式偏好"调用者提供缓冲区"的接口？', '二级指针还有哪些典型用途？']
},
{
  id: 'code-014', cat: 'coding', type: 'qa', level: 2, tags: ['CRC'],
  q: '手写一个 CRC-16/MODBUS 的计算函数。',
  a: '**逐位计算版（代码短，无需查表）**：\n```c\nuint16_t crc16_modbus(const uint8_t *data, size_t len)\n{\n    uint16_t crc = 0xFFFF;                  /* 初始值 */\n    while (len--) {\n        crc ^= *data++;                     /* 与当前字节异或（低字节）*/\n        for (int i = 0; i < 8; i++) {\n            if (crc & 0x0001)\n                crc = (crc >> 1) ^ 0xA001;  /* 多项式的反射形式 */\n            else\n                crc >>= 1;\n        }\n    }\n    return crc;                             /* Modbus 无最终异或 */\n}\n```\n\n**查表版（快 8 倍，代价是 512 字节表）**：\n```c\nstatic uint16_t table[256];\n\nvoid crc16_init(void)\n{\n    for (uint16_t i = 0; i < 256; i++) {\n        uint16_t c = i;\n        for (int k = 0; k < 8; k++)\n            c = (c & 1) ? ((c >> 1) ^ 0xA001) : (c >> 1);\n        table[i] = c;\n    }\n}\n\nuint16_t crc16_fast(const uint8_t *data, size_t len)\n{\n    uint16_t crc = 0xFFFF;\n    while (len--)\n        crc = (crc >> 8) ^ table[(crc ^ *data++) & 0xFF];\n    return crc;\n}\n```\n\n**必须说清楚的几个参数**（这才是这题的考点）：\n\nCRC 由**五个参数**唯一确定，任一不同就是不同的 CRC：\n| 参数 | Modbus 的值 |\n|---|---|\n| 多项式 | 0x8005（反射形式 **0xA001**）|\n| 初始值 | **0xFFFF** |\n| 输入反射 | 是 |\n| 输出反射 | 是 |\n| 最终异或 | 0x0000 |\n\n**因为输入输出都反射，所以实现上用"右移 + 反射多项式 0xA001"**，等价于"左移 + 0x8005 + 手工反射"，但前者代码更简洁。\n\n**联调时 CRC 对不上，99% 是这五个参数有一个不一致**——用在线 CRC 计算器逐项对照是最快的排查方式，比读对方代码快得多。\n\n**Modbus 特有的一个坑**：算出的 CRC 在报文里是**低字节在前、高字节在后**（小端），和大多数协议相反。写成：\n```c\nframe[len]     = crc & 0xFF;        /* CRC 低字节 */\nframe[len + 1] = (crc >> 8) & 0xFF; /* CRC 高字节 */\n```\n**这个顺序搞反是 Modbus 调试最常见的错误之一。**\n\n**工程建议**：很多 MCU 有硬件 CRC 单元，配置对参数后几乎零 CPU 开销。做固件校验这类大数据量计算时应该优先用它。另外要明确 **CRC 只防意外错误，不防恶意篡改**——攻击者能轻易构造出 CRC 相同的数据。',
  followup: ['为什么反射的 CRC 用右移实现？', 'Modbus 的 CRC 字节序为什么和别的协议相反？', '硬件 CRC 单元怎么配置成 Modbus 参数？']
},
{
  id: 'code-015', cat: 'coding', type: 'qa', level: 2, tags: ['字符串', 'sprintf'],
  q: '不使用 printf 系列函数，写一个把 uint32_t 转成十六进制字符串的函数。',
  a: '```c\n/* 把 val 转成 8 位十六进制字符串（不足补 0），写入 buf，返回长度 */\nint u32_to_hex(uint32_t val, char *buf, size_t size)\n{\n    static const char hex[] = \"0123456789ABCDEF\";\n\n    if (buf == NULL || size < 9) return -1;   /* 8 位 + 结束符 */\n\n    for (int i = 7; i >= 0; i--) {\n        buf[i] = hex[val & 0x0F];             /* 取最低 4 位查表 */\n        val >>= 4;\n    }\n    buf[8] = \'\\0\';\n    return 8;\n}\n```\n\n**不补零的版本（去掉前导 0）**：\n```c\nint u32_to_hex_min(uint32_t val, char *buf, size_t size)\n{\n    static const char hex[] = \"0123456789ABCDEF\";\n    char tmp[8];\n    int n = 0;\n\n    if (val == 0) { tmp[n++] = \'0\'; }\n    while (val) { tmp[n++] = hex[val & 0x0F]; val >>= 4; }\n\n    if (buf == NULL || size < (size_t)n + 1) return -1;\n\n    for (int i = 0; i < n; i++)\n        buf[i] = tmp[n - 1 - i];              /* 反转，因为是从低位开始生成的 */\n    buf[n] = \'\\0\';\n    return n;\n}\n```\n\n**要点**：\n\n1. **查表比条件判断快也更简洁**。`hex[val & 0x0F]` 一步到位，不用写 `if (d < 10) c = d + \'0\'; else c = d - 10 + \'A\';`。\n\n2. **`& 0x0F` 和 `>>= 4`** 分别取和移一个十六进制位（4 bit）。用位运算而不是 `% 16` 和 `/ 16`——**很多 MCU 没有硬件除法器**。\n\n3. **缓冲区大小必须检查**。这是所有字符串函数的铁律。\n\n4. **定长版本从高位往低位填**（不需要反转），**变长版本从低位开始生成所以要反转**。这个差异是这题的小陷阱。\n\n5. **`static const` 的查表数组**加了 const 才会放进 .rodata（Flash），不加会占 RAM。\n\n**为什么嵌入式要自己写这个**：\n\n- **`printf` 太重**：链接完整的格式化代码可能占几十 KB Flash，栈开销上 KB\n- **`printf` 不可重入**，中断里绝对不能用\n- **耗时不确定**，实时路径上不可接受\n\n所以很多项目会实现一套精简的输出函数：`u32_to_hex`、`u32_to_dec`、`str_append`，组合起来拼日志。或者用 `nanoprintf`、`tinyprintf` 这类几 KB 的精简实现。\n\n**更进一步的做法**：**二进制日志**——不在设备端做格式化，只输出"格式串 ID + 参数的二进制值"，由上位机对照符号表还原。Flash 占用、带宽和执行时间都能降一个数量级（SEGGER RTT + SystemView 就是这个思路）。',
  followup: ['为什么 printf 的栈开销那么大？', '二进制日志怎么实现？', '中断里想输出调试信息该怎么办？']
},
{
  id: 'code-016', cat: 'coding', type: 'qa', level: 3, tags: ['指针', '回调'],
  q: '设计一个支持多个订阅者的事件回调机制（C 语言，不用动态内存）。',
  a: '```c\n#define MAX_HANDLERS 8\n\ntypedef void (*event_cb_t)(uint32_t event, void *arg, void *user_data);\n\ntypedef struct {\n    event_cb_t cb;\n    void      *user_data;\n    uint32_t   mask;          /* 关心哪些事件（位掩码）*/\n} handler_t;\n\ntypedef struct {\n    handler_t list[MAX_HANDLERS];\n    uint8_t   count;\n} event_bus_t;\n\nint bus_subscribe(event_bus_t *bus, event_cb_t cb, uint32_t mask, void *ud)\n{\n    if (cb == NULL) return -1;\n    if (bus->count >= MAX_HANDLERS) return -1;    /* 静态上限，不扩容 */\n\n    bus->list[bus->count].cb        = cb;\n    bus->list[bus->count].mask      = mask;\n    bus->list[bus->count].user_data = ud;\n    bus->count++;\n    return 0;\n}\n\nvoid bus_publish(event_bus_t *bus, uint32_t event, void *arg)\n{\n    for (uint8_t i = 0; i < bus->count; i++) {\n        if (bus->list[i].mask & (1u << event))\n            bus->list[i].cb(event, arg, bus->list[i].user_data);\n    }\n}\n```\n\n**设计要点**：\n\n**1. `user_data` 参数不能少**\nC 没有闭包，回调函数拿不到调用者的上下文。**传一个 `void *user_data` 让订阅者把自己的对象指针带进来**，回调里再转回去：\n```c\nstatic void my_handler(uint32_t ev, void *arg, void *ud) {\n    my_module_t *self = (my_module_t *)ud;   /* 拿回上下文 */\n    ...\n}\n```\n这是 C 里实现"面向对象回调"的标准手法。**没有这个参数，回调就只能操作全局变量**，模块就无法多实例化。\n\n**2. 静态数组而不是链表 + malloc**\n- 数量上限编译期确定，**RAM 用量可核算**\n- 不会分配失败\n- 遍历时缓存友好\n- 符合 MISRA C 对动态内存的限制\n\n**3. 事件掩码过滤**\n避免每个订阅者都被调用后再自己判断"这个事件我关不关心"，减少无谓的函数调用。\n\n**4. 必须考虑的几个问题（面试时主动说）**：\n\n- **回调在什么上下文执行？** 如果 `bus_publish` 是在中断里调用的，那所有回调都在中断上下文，**不能阻塞、不能耗时**。这一点必须在接口文档里写清楚，否则订阅者会误用\n- **回调里能不能反过来 subscribe/unsubscribe？** 会破坏正在遍历的数组。要么禁止（文档约定 + assert），要么用"延迟处理"（先记录待删除项，遍历完再统一处理）\n- **一个回调崩了会影响后面的吗？** C 里没有异常，一个回调里的野指针会直接搞垮整个系统。所以回调必须是可信代码\n- **需要取消订阅吗？** 加一个 `bus_unsubscribe`，用交换末尾元素的方式删除（O(1)，但会打乱顺序）\n\n**对比 C++ 的做法**：`std::function` + `std::vector` 更方便，但 `std::function` 可能有堆分配、vector 会动态扩容——**嵌入式上这个 C 风格的静态实现更可控**。\n\n**这道题的价值**：它同时考了函数指针、void* 的用法、静态资源管理，以及最重要的——**接口设计时对上下文和边界的思考**。',
  followup: ['为什么 C 的回调必须带 user_data 参数？', '回调里再修改订阅列表会有什么问题？', '怎么支持取消订阅？']
}
]);
